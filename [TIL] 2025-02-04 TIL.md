# TIL 250204

**다우기술 FITs 3기 임광영**

**학습 날짜 : 2025년 02월 04일 화요일**

---

## Java 특징

### `JVM`
일반적인 언어는 High-Language로 작성한 코드를 컴파일러를 통해서 머신 코드로 변경한다.

반면 Java는 컴파일러를 통해서 바이트 코드로 변경하고, 이를 JVM이 읽어 머신 코드로 변경하여 실행한다. 즉, OS 위에 `JVM`만 있으면 어디서든 Java는 실행 가능하다.

Java는 운영체제에 독립적이지만 `JVM`은 운영체제에 종속적이다.

> `.java` 파일 (High-Language)
$\rightarrow$ `javac compiler`
$\rightarrow$ `.class` 파일 (Bytecode)
$\rightarrow$ `JVM`

### 객체지향
절차적 언어의 대표적으로는 C가 있다. 더이상 분할할 수 없는 단위 기능이 나올 때까지 프로그램을 세분화시켜 작동한다. 이러한 단위 기능을 함수로 구현한다.
설계와 구현이 쉬워지며 결과적으로는 비용적인 이점이 생긴다. 하지만 유지보수와 재사용이 어렵다.

> Python과 JavaScript는 절차적 언어이지만, 객체지향의 성격도 채택하고 있다.

이 문제를 해결하기 위헤 프로그래밍 패러다임이 변화했으며 객체지향 프로그래밍이 탄생했다. 해결해야 하는 현실 세계를 프로그램으로 모델링하는 작업을 한 후에 프로그래밍을 진행한다.
해결해야 하는 문제를 구성하고 있는 구성요소를 파악하고 프로그램으로 표현한다. 이에 추상화(Abstraction)를 거쳐서 나온 여러가지 정보를 상태와 행위로 구분한다. 상태는 변수(Fields)로, 행위는 함수(Methods)로 표현하며, Fields와 Methods를 묶어서 클래스(Class)라 부른다.

따라서 클래스라는 단위는 객체 모델링을 하기 위한 수단으로 활용되며, 결국 Java 프로그램은 클래스의 집합으로 표현될 수 있다. 일반적으로 코드로 표현할 때 클래스 한 개당 파일 한 개이며 (Public) 클래스의 이름을 파일명으로 명명한다.

해당 클래스로부터 저장 공간을 생성하고 이를 활용해 프로그래밍을 진행한다. 이때 저장 공간을 Instance라고 한다.
클래스로부터 만들어진 Instance는 Fields의 데이터 타입에 따라 메모리 크기가 달라진다. 즉, 클래스는 데이터 타입의 역할을 수행하며 고정된 데이터 타입이 아니기 때문에 추상 데이터 타입(Abstract Data Type, ADT)이라고 부른다.

> Instance와 같은 동적인 메모리는 힙(Heap)에 저장된다.

유지보수와 재사용성에 이점이 있지만, 프로그램의 설계와 작성이 어렵다.

Java는 클래스의 집합으로 표현되며 하나의 프로그램 내에는 상당히 많은 클래스들이 존재한다. 이러한 클래스를 효율적으로 관리하기 위해 패키지(Package)라는 논리적인 개념(단위)을 사용한다. 이는 물리적으로 폴더로 표현된다.

```java
// Package 명은 소문자로 작성한다.
// Java 파일을 Package 폴더 내에 위치한다.
package 패키지명;

public class 클래스명 {
    // Fields
    // 변수
    Data_type 변수명;

    // 생성자(Constructor)
    // 인스턴스를 생성하기 위해 필요하다.
    // 메서드와는 다르다.
    public 클래스명 ( 파라미터1, 파라미터2, ... ) {
    	// 생성자 내부 구현
    }

    // Methods
    // 함수
    public Return_type 메소드명 ( 파라미터1, 파라미터2, ... ) {
    	// 메소드 내부 구현
    }
}
```

> ### IntelliJ 단축키
`main` + `Tab` : `public static void main(String[] args) {}`
`sout` + `Tab` : `System.out.println();`

## `JVM`, `JRE`, `JDK`

### `JVM`
Java Virtual Machine의 약자이다. Bytecode를 읽어들여서 실행하는 실행 주체이다.

### `JRE`
Java Runtime Environment의 약자로, Java 실행 환경이다. Java 개발을 위한 여러 클래스 라이브러리와 API를 지원한다.

`JRE` = `JVM` + Java Class Library

### `JDK`
Java Development Kit의 약자로, Java 개발 도구이다.

`JDK` = `JRE` + 자바 개발 도구(`javac`, `javadoc` 등)

![](https://velog.velcdn.com/images/pyoung/post/84b7fb9d-11df-448e-a5b7-8a6b4fb33e65/image.png)

## Data Type

### Data Type
- Java는 Strong Type 언어.
- 변수의 대소문자 구분을 하며 길이의 제한 없음.
- 변수명으로 예약어 불가.
- 숫자로 시작하는 변수명 불가.
- 특수문자는 `_`와 `$`만 허용.
- 클래스 이름은 PascalCase이며 일반 변수 이름은 camelCase.

> Python은 snake_case.

Java의 데이터 타입은 크게 두 가지이다.
- Primitive Data Type.
  - 8개로 정해져있음.
  - 해당 타입으로 선언된 변수 안에 직접 데이터를 저장.
  - 해당 타입은 Stack과 Heap에 상관없이 모두에 저장되며, 단순히 참조가 아닌 값을 직접 저장.
  - 정수형 : `byte`(1 byte), `short`(2 byte), `int`(4 byte), `long`(8 byte).
  - 실수형 : `float`, `double`.
  - 문자형 : `char`.
  - 논리형 : `boolean`.
- Reference Data Type.
  - 클래스로 선언된 변수를 지칭.
  - 해당 타입은 메모리 주소(Hash 값)를 저장.

### 상수(Constant)
한 번 값을 저장하면 다른 값으로 변경할 수 없으며 `final` 키워드를 통해 표현한다. 일반적으로 대문자와 `_`를 사용하여 선언한다.

```java
final int MAX_SPEED = 100;
```

### 형 변환(Type Casting)
하나의 데이터 타입이 다른 데이터 타입으로 변환되는 것을 의미한다.
- 묵시적(implicit) 형 변환.
  ```java
  public class TypeCasting {
      public static void main(String[] args) {
          double k = 10 + 20.0;
          System.out.println(k);  // 30.0
      }
  }
  ```
- 명시적(casting) 형 변환 : `(타입)피연산자`를 통해 형 변환.
  ```java
  public class TypeCasting {
      public static void main(String[] args) {
          double phi = 3.141592358979;
          int myData = (int)phi;
          System.out.println(myData);  // 3
      }
  }
  ```

### Java 프로그램 동작

```java
public class Customer {
    // 기본 생성자(Default Constructor) : 매개변수가 없는 생성자
    // 작성하지 않으면 Java가 자동적으로 생성한다.
    // 모든 클래스는 생성자를 갖는다.
    public Customer() {
    }

    // Field
    // int는 4 byte
    int age;  // Field

    // Method
    public void myprint() {
        System.out.println("Hello World");
    }

    public static void main(String[] args) {
        int age = 100;  // Local Variable

        // 클래스로부터 인스턴스를 생성하기 위한 키워드
        // new 키워드 뒤에는 반드시 생성자가 온다.
        Customer customer = new Customer();
    }
}
```

- `JVM` 메모리 구조는 Method Area, Stack, Heap으로 구분.
- 해당 파일을 실행하면 Method Area에 `Customer` 클래스와 함께 `age`와 `myprint()`, `main()`의 정보를 저장.
- `main()` 함수를 먼저 찾음.
  - 지역 변수 `age`를 Stack에 저장.
  - 인스턴스 `customer`을 생성함.
  - Stack에 지역 변수이자 Reference 자료형인 `customer`를 저장.
  - Heap에 `age`와 `myprint()`의 주소를 저장.
  - Stack의 `customer`는 Heap의 `age`, `myprint()`을 참조.
- `main()` 함수가 끝나면 Stack에 저장된 `age`와 `customer`를 지움.
  - `garbage collector`가 Heap 영역에 있는 `age`와 `myprint()`를 지움.
- Method Area에 저장된 `age`, `myprint()`, `main()`과 함께 `Customer` 클래스를 지움.

### 객체지향 예제
```java
package mypackage;

public class InstanceTest {
    // 기본적으로 Field와 Method는 인스턴스를 생성할 때 사용한다.
    // static 키워드를 사용하면 인스턴스 없이도 사용할 수 있다.
    // static 키워드를 사용하면 Method Area에 정보가 아닌 공간이 할당된다.

    // Static Field or Static Variable or Class Variable
    static int a = staticCall("1번");
    // Instance Variable
    int b = staticCall("2번");

    // Static Method
    public static int staticCall(String msg) {
        System.out.println(msg);
        return 100;
    }

    // Default Constructor
    // 기본 생성자는 명시적으로 작성한다.
    // 생성자는 인스턴스를 생성하기 위해서 반드시 호출되고
    // new 키워드에 의해 만들어진 인스턴스를 초기화하는 역할을 수행한다.
    public InstanceTest() {
        this.b = staticCall("3번");
    }

    public static void main(String[] args) {
        System.out.println("4번");
        int c = staticCall("5번");
        InstanceTest d = new InstanceTest();
    }
}
```
- 해당 파일을 실행하면 `InstanceTest` 인스턴스와 다음 내용들을 저장.
  - Method Area에 `a`와 `staticCall()`에 해당하는 공간이 할당.
  - `b`와 `InstanceTest()`, `main()`에 해당하는 정보를 저장.
- `staticCall`의 공간이 할당됨에 따라 사용할 수 있으므로 호출.
  - `1번` 출력.
- `main()` 함수 실행.
  - `4번` 출력.
- Stack 영역에 지역 변수 `c`에 해당하는 공간이 할당.
  - `staticCall("5번")`이 실행.
  - `5번` 출력.
- Stack 영역에 지역 변수이자 참조형 데이터타입인 `InstanceTest`를 저장.
- Heap 영역에 `InstanceTest`에 해당하는 공간이 할당.
- `new` 키워드에 의해 `InstanceTest`를 초기화하며, 이를 위해 Heap 영역에서 우선 인스턴스 `b`가 실행.
  - `2번` 출력.
  - `3번` 출력.

## Overloading

### Overloading
같은 이름이나 형태로 여러 개의 기능을 수행하는 경우를 overloading이 발생했다고 한다.

예를 들어 `+` 연산자는 두 숫자를 더하지만, 문자열이라면 둘을 잇는다. 이 개념을 메소드에도 적용한 것이 Method Overloading이다.

이를 생성자에 적용하면 Constructor Overloading이다.