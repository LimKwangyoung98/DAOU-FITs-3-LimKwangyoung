# TIL 250204

**다우기술 FITs 3기 임광영**

**학습 날짜 : 2025년 02월 04일 화요일**

---

## Java 특징

### `JVM`
일반적인 언어는 High-Language로 작성한 코드를 컴파일러를 통해서 머신 코드로 변경한다.

반면 Java는 컴파일러를 통해서 	Bytecode로 변경하고, 이를 `JVM`이 읽어 머신 코드로 변경하여 실행한다. 즉, OS 위에 `JVM`만 있으면 어디서든 Java는 실행 가능하다.
Java는 운영체제에 독립적이지만 `JVM`은 운영체제에 종속적이다.

> `.java` 파일 (High-Language)
$\rightarrow$ `javac compiler`
$\rightarrow$ `.class` 파일 (Bytecode)
$\rightarrow$ `JVM`

## `JVM`, `JRE`, `JDK`

### `JVM`
Java Virtual Machine의 약자이다. Bytecode를 읽어들여서 실행하는 실행 주체이다.

### `JRE`
Java Runtime Environment의 약자로, Java 실행 환경이다. Java 개발을 위한 여러 클래스 라이브러리와 API를 지원한다.

`JRE` = `JVM` + Java Class Library

### `JDK`
Java Development Kit의 약자로, Java 개발 도구이다.

`JDK` = `JRE` + 자바 개발 도구(`javac`, `javadoc` 등)

![](https://velog.velcdn.com/images/pyoung/post/84b7fb9d-11df-448e-a5b7-8a6b4fb33e65/image.png)

## 객체지향 프로그래밍

### 절차적 프로그래밍
절차적 언어에는 대표적으로 C가 있다. 이는 더이상 분할할 수 없는 단위 기능이 나올 때까지 프로그램을 세분화시켜 작동한다. 이러한 단위 기능을 함수로 구현한다.

설계와 구현이 쉬워지며 결과적으로는 비용적인 이점이 생긴다. 하지만 유지보수와 재사용이 어렵다.

> Python과 JavaScript는 절차적 언어이지만, 객체지향의 성격도 채택하고 있다.

### 객체지향 프로그래밍
절차적 프로그래밍의 문제를 해결하기 위해 프로그래밍 패러다임이 변화했으며 객체지향 프로그래밍이 탄생했다. 해결해야 하는 현실 세계를 프로그램으로 모델링하는 작업을 한 후에 프로그래밍을 진행한다. 유지보수와 재사용성에 이점이 있지만, 프로그램의 설계와 작성이 어렵다.

해결해야 하는 문제를 구성하고 있는 구성요소를 파악하고 프로그램으로 표현한다. 이에 추상화(Abstraction)를 거쳐서 나온 여러가지 정보를 상태와 행위로 구분한다. 상태는 변수(Fields)로, 행위는 함수(Methods)로 표현하며, Fields와 Methods를 묶어서 클래스(Class)라 부른다.

따라서 클래스라는 단위는 객체 모델링을 하기 위한 수단으로 활용되며, 결국 Java 프로그램은 클래스의 집합으로 표현될 수 있다. 일반적으로 코드로 표현할 때 클래스 한 개당 파일 한 개이며 (Public) 클래스의 이름을 파일명으로 명명한다.

### ADT
클래스로부터 저장 공간을 생성하고 이를 활용해 프로그래밍을 진행한다. 이때 저장 공간을 Instance라고 한다.

클래스로부터 만들어진 Instance는 Fields의 데이터 타입에 따라 메모리 크기가 달라진다. 즉, 클래스는 데이터 타입의 역할을 수행하며 고정된 데이터 타입이 아니기 때문에 추상 데이터 타입(Abstract Data Type, ADT)이라고 부른다.

> Instance와 같은 동적인 메모리는 힙(Heap)에 저장된다.

### Java 프로그래밍의 특징
Java는 클래스의 집합으로 표현되며 하나의 프로그램 내에는 상당히 많은 클래스들이 존재한다. 이러한 클래스를 효율적으로 관리하기 위해 패키지(Package)라는 논리적인 개념(단위)을 사용한다. 이는 물리적으로 폴더로 표현된다.

```java
// Package 명은 소문자로 작성한다.
// Java 파일을 Package 폴더 내에 위치한다.
package 패키지명;

public class 클래스명 {
    // Fields
    // 변수
    Data_type 변수명;

    // 생성자(Constructor)
    // 인스턴스를 생성하기 위해 필요하다.
    // 메서드와는 다르다.
    public 클래스명 ( 파라미터1, 파라미터2, ... ) {
    	// 생성자 내부 구현
    }

    // Methods
    // 함수
    public Return_type 메소드명 ( 파라미터1, 파라미터2, ... ) {
    	// 메소드 내부 구현
    }
}
```

> ### IntelliJ 단축키
`main` + `Tab` : `public static void main(String[] args) {}`
`sout` + `Tab` : `System.out.println();`

## Data Type

### variable 명명 규칙
- Java는 Strong Type 언어.
- 변수의 대소문자 구분을 하며 길이의 제한 없음.
- 변수명으로 예약어 불가.
- 숫자로 시작하는 변수명 불가.
- 특수문자는 `_`와 `$`만 허용.
- 클래스 이름은 PascalCase이며 일반 변수 이름은 camelCase.

> Python은 snake_case.

### Data Type
Java의 데이터 타입은 크게 두 가지이다.
- Primitive Data Type.
  - 8개로 정해져있음.
  - 해당 타입으로 선언된 변수 안에 직접 데이터를 저장.
  - 해당 타입은 Stack과 Heap에 상관없이 모두에 저장되며, 단순히 참조가 아닌 값을 직접 저장.
  ![](https://velog.velcdn.com/images/pyoung/post/435e7ccd-c8d7-4a2d-ad80-796b3a17a2a6/image.png)
- Reference Data Type.
  - 클래스로 선언된 변수를 지칭.
  - 해당 타입은 메모리 주소(Hash 값)를 저장.

### 상수(constant)
한 번 값을 저장하면 다른 값으로 변경할 수 없으며 `final` 키워드를 통해 표현한다. 일반적으로 대문자와 `_`를 사용하여 선언한다.

```java
final int MAX_SPEED = 100;
```

### 형 변환(Type Casting)
하나의 데이터 타입이 다른 데이터 타입으로 변환되는 것을 의미한다.
- 묵시적(implicit) 형 변환.
  ```java
  public class TypeCasting {
      public static void main(String[] args) {
          double k = 10 + 20.0;
          System.out.println(k);  // 30.0
      }
  }
  ```
- 명시적(casting) 형 변환 : `(타입)피연산자`를 통해 형 변환.
  ```java
  public class TypeCasting {
      public static void main(String[] args) {
          double phi = 3.141592358979;
          int myData = (int)phi;
          System.out.println(myData);  // 3
      }
  }
  ```

## Java 프로그램 동작

### `JVM` 메모리 구조
`JVM` 메모리 구조는 Method Area, Stack, Heap 영역으로 구분된다.

**Method Area**
`JVM`이 클래스를 올릴 때 클래스 수준의 정보를 저장하는 공간으로, `static` 변수와 `static` 메서드, 메서드 코드가 저장된다.

저장되는 데이터는 다음과 같다.
- 클래스 정보 : 클래스 이름, 상속 정보, 인터페이스 정보 등.
- `static` 변수 및 메서드 : 클래스가 로드될 때 저장.
- 메서드 코드.

**Stack**
메서드 실행 시마다 생성되는 Call Stack을 저장하는 공간으로, 지역변수와 매개변수, 주소 등이 저장된다. 메서드가 종료되면 해당 스택 프레임이 제거된다.

저장되는 데이터는 다음과 같다.
- 메서드 호출 스택 프레임.
- 지역변수 및 매개변수.
- 반환 주소.

**Heap**
인스턴스 및 인스턴스 변수가 저장되는 공간으로, `new` 키워드로 생성된 인스턴스는 모두 Heap에 저장된다. Garbage Collector가 사용하지 않는 인스턴스를 자동으로 해제한다.

저장되는 데이터는 다음과 같다.
- 인스턴스.
- 인스턴스 변수.

### 예제 1
```java
public class Customer {
    // Field
    int age;  // 4 byte

    // Method
    public void myprint() {
        System.out.println("Hello World");
    }
    
	// Default Constructor
    public Customer() {
    }

    public static void main(String[] args) {
        int age = 100;  // Local Variable
        Customer customer = new Customer();
    }
}
```

- `Customer` 클래스와 필드 `age`, 메서드 `myprint()`, 생성자 `Customer()`, `main()`의 정보를 **Method Area**에 로드.
- `main()` 함수를 실행하며 **Stack**에 `main()`의 스택 프레임 생성.
  - 지역변수 `age` 로드.
- `new` 키워드를 통해 **Heap**에 `Customer` 인스턴스 생성.
  - 인스턴스 초기화.
  - **Heap**에 `age`와 `myprint()` 저장.
  - **Stack**에 `customer` 참조변수 저장 및 **Heap**의 `Customer` 인스턴스 참조.
- `main()` 종료 시 **Stack**의 `main()` 프레임 제거.
- Garbage Collector가 **Heap**의 `Customer` 인스턴스 제거.
- **Method Area**의 `Customer` 클래스 제거.

![](https://velog.velcdn.com/images/pyoung/post/1417ee09-2f39-42ab-84ec-b13a7aa18a61/image.png)


### 예제 2
```java
public class InstanceTest {
    // Static Field or Static Variable or Class Variable
    static int a = staticCall("1번");
    // Instance Variable
    int b = staticCall("2번");

    // Static Method
    public static int staticCall(String msg) {
        System.out.println(msg);
        return 100;
    }

    // Default Constructor
    public InstanceTest() {
        this.b = staticCall("3번");
    }

    public static void main(String[] args) {
        System.out.println("4번");
        int c = staticCall("5번");
        InstanceTest d = new InstanceTest();
    }
}
```
- `InstanceTest` 클래스가 **Method Area**에 로드.
  - `static` 변수 `a`와 `static` 메서드 `staticCall()`에 해당하는 공간 할당.
  - 필드 `b`와 생성자 `InstanceTest()`, `main()`의 정보 로드.
- `staticCall()`의 공간이 할당됨에 따라 해당 메서드 실행.
  - **`1번`** 출력.
  - `static` 변수 `a`에 `100` 저장.
- `main()` 함수를 실행하며 **Stack**에 `main()`의 스택 프레임 생성.
  - **`4번`** 출력.
  - `staticCall()`이 실행됨에 따라 **`5번`** 출력.
  - 지역변수 `c` 로드 및 `100` 저장.
- `new` 키워드를 통해 **Heap**에 `InstanceTest` 인스턴스 생성.
  - 인스턴스 초기화.
  - `b`가 초기화됨에 따라 `staticCall()` 실행.
  - **`2번`** 출력.
  - **Heap**에 `b` 저장 및 `100` 저장.
  - **Stack**에 `d` 참조변수 저장 및 **Heap**의 InstanceTest 인스턴스 참조.
- 생성자 `InstanceTest()` 실행.
  - `staticCall()`이 실행됨에 따라 **`3번`** 출력.
  - `b`에 `100` 저장.
- `main()` 종료 시 **Stack**의 `main()` 프레임 제거.
- Garbage Collector가 **Heap**의 `InstanceTest` 인스턴스 제거.
- **Method Area**의 `Instance` 클래스 제거.

![](https://velog.velcdn.com/images/pyoung/post/bcda9b5b-57c0-4078-b24e-672260f79150/image.png)

> ### 기본 생성자(Default Constructor)
매개변수가 없는 생성자이다. 모든 클래스는 생성자를 가지며, 작성하지 않으면 Java가 자동적으로 생성한다. 하지만 명시적으로 작성하는 것이 좋다.
생성자는 인스턴스를 생성하기 위해서 반드시 호출되며, `new` 키워드에 의해 생성된 인스턴스를 초기화하는 역할을 수행한다.

> ### `static`
기본적으로 Field와 Method는 **Heap**에 인스턴스가 생성된 후에 사용할 수 있다. 다만 `static` 키워드를 사용하면 **Method Area**에 정보가 아닌 공간이 바로 할당되며, 인스턴스 생성 없이 사용할 수 있다.

## Overloading

### Overloading
같은 이름이나 형태로 여러 개의 기능을 수행하는 경우를 말한다.

예를 들어 `+` 연산자는 두 숫자를 더하지만, 문자열이라면 둘을 잇는다. 이 개념을 메소드에도 적용한 것이 Method Overloading이다. 이를 생성자에 적용하면 Constructor Overloading이다.