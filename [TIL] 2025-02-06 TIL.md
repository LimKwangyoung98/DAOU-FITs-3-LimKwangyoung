# TIL 250206

**다우기술 FITs 3기 임광영**

**학습 날짜 : 2025년 02월 06일 목목요일**

---

## 상속

### Constructor의 상속
생성자는 상속되지 않는다. 하위 클래스의 객체를 생성할 경우 먼저 상위 클래스의 객체가 만들어져야 하기 때문에 상위 클래스의 생성자를 호출한다. 이는 명시하지 않아도 자동으로 상위 클래스의 생성자를 호출한다.
이때 자동으로 호출되는 상위 클래스의 생성자는 인자 없는 생성자로 호출된다.

```java
// Main.java

public class Main {
    public static void main(String[] args) {
        Student s = new Student();
    }
}
```
```java
// Person.java

// 상위 클래스
class Person {
    // Fields
    String name;
    String phone;
    String address;  // 해당 필드 추가 시 하위 클래스에도 자동 추가

    // Constructor
    public Person() {
    	System.out.println("Person 생성자 호출");
    }

    // Methods
    public void sleep() {
    }

    public void eat() {
    }
}
```
```java
// Student.java

class Student extends Person {
    // Fields
    String dept;

    // Constructor
    public Student() {
        // 상위 클래스 Person의 생성자를 호출한다.
        // super() 키워드를 작성하지 않아도 자동으로 호출한다.
        // super() 키워드 이전에는 어떠한 동작도 수행할 수 없다.
        super();
        System.out.println("Student 생성자 호출");
    }

    // Methods
    public void study() {
    }
}

```

해당 코드를 실행하면 다음과 같다.
- `Student` 인스턴스를 생성하고자 함
- `Student` 클래스의 상위 클래스인 `Person` 클래스의 인스턴스 생성하고자 함
  - `Student` 클래스의 생성자에서 `super()` 키워드를 통해 상위 클래스 `Person`의 생성자 호출.
  - `super()` 키워드를 작성하지 않아도 자동으로 호출.
- `Person` 클래스의 인스턴스를 생성
  - 최상위 클래스인 `java.lang.Object`의 인스턴스 생성 및 상속.
- `Student` 클래스의 인스턴스를 생성
  - 상위 클래스인 `Person` 상속.
  
![](https://velog.velcdn.com/images/pyoung/post/75892f09-7bf2-461a-9515-f18feee629b5/image.png)

> `Student` 클래스에 `name` 필드가 있다면, 이는 `Person` 클래스의 `name` 필드를 덮는다. 이를 Shadow라고 한다.

### Method Overriding
Overriding이란 상위 클래스로부터 상속받는 메소드의 내용을 하위 클래스에서 다시 정의하는 것을 의미한다.

Method Overriding의 조건은 다음과 같다.
- 메소드의 이름 동일
- 메소드의 반환 타입 동일
- 메소드의 파라미터 개수와 타입 동일
- Access Modifier은 부모 클래스의 메소드와 같거나 더 넓어야 함

```java
// Student.java

class Student extends Person {
    // Fields
    String dept;

    // Constructor
    public Student() {
        // 상위 클래스 Person의 생성자를 호출한다.
        // super() 키워드를 작성하지 않아도 자동으로 호출한다.
        // super() 키워드 이전에는 어떠한 동작도 수행할 수 없다.
        super();
        System.out.println("Student 생성자 호출");
    }

    // Methods
    public void study() {
    }

    @Override
    public void sleep() {
    }
}
```

![](https://velog.velcdn.com/images/pyoung/post/b34ffbce-3d2c-4d40-939b-1fe7d5a7982b/image.png)


### IS-A Relationship
`Sub class is a Super class`.

하위 클래스의 타입을 써야 하는 자리에 상위 클래스의 타입을 사용해도 된다. 이를 Type Casting이라 한다.

```java
// Main.java

public class Main {
    public static void main(String[] args) {
    	// Studnet s = new Student();
        Person s = new Student();
    }
}
```

![](https://velog.velcdn.com/images/pyoung/post/2f957dca-c7ec-4f43-a53c-7a5fa5876653/image.png)

> 역은 성립하지 않는다.

### `this`, `super`, `this()`, `super()`
- `this`
  - 해당 키워드가 사용된 인스턴스에 대한 참조.
- `super`
  - 해당 키워드가 사용된 인스턴스의 상위 클래스의 인스턴스.
- `this()`
  - 생성자 내에서 Overloading된 다른 생성자를 호출하기 위해 사용.
- `super()`
  - 생성자 내 상위 클래스의 생성자를 호출하기 위해 사용.

```java
// Main.java

public class Main {
	public static void main(String[] args) {
		Student s = new Student();
	}
}
```
```java
// Person.java

// 상위 클래스
class Person {
    // Fields
    String name;

    // Constructor
    public Person() {
    }

    // Methods
    public void eat() {
    }
}
```
```java
// Student.java

class Student extends Person {
    // Fields
    String dept;

    // Constructor
    public Student(String name) {
        System.out.println(name);  // 둘리
        System.out.println("다른 생성자가 호출됩니다.");
    }

    public Student() {
        this("둘리");           // 다른 생성자 Student()
        this.dept = "철학과";   // Student 클래스의 인스턴스
        super.name = "홍길동";  // Person 클래스의 인스턴스
    }

    // Methods
    @Override
    public void eat() {
    }

    public void study() {
        System.out.println("학생은 공부해야 합니다.");
    }
}
```

### 상속 예제
```java
// SuperClass.java

class SuperClass {
    static int staticCall(String msg) {
        System.out.println(msg);
        return 0;
    }

    int a = staticCall("1번 문장입니다.");
    static int b = staticCall("2번 문장입니다.");

    public SuperClass() {
        staticCall("3번 문장입니다.");
    }

    public SuperClass(int i) {
        this();
        staticCall("4번 문장입니다.");
    }

    public void myFunc() {
        System.out.println("5번 문장입니다.");
    }
}
```
```java
// Exam01_InheritanceOrder.java

public class Exam01_InheritanceOrder extends SuperClass {
    int c = staticCall("6번 문장입니다.");
    static int d = staticCall("7번 문장입니다.");

    public Exam01_InheritanceOrder() {
        super(100);
        staticCall("8번 문장입니다.");
        super.myFunc();
    }

    @Override
    public void myFunc() {
        System.out.println("9번 문장입니다.");
    }

    public static void main(String[] args) {
        System.out.println("10번 문장입니다.");
        SuperClass obj = new Exam01_InheritanceOrder();
        obj.myFunc();
    }
}
```
- JVM이 `SuperClass`를 로드
  - **`2번 문장입니다.`** 출력.
- JVM이 `Exam01_InheritanceOrder`를 로드
  - **`7번 문장입니다.`** 출력.
- **`10번 문장입니다.`** 출력.
- `Exam01_InheritanceOrder` 클래스의 인스턴스 생성
  - 부모 클래스 `SuperClass`의 인스턴스 먼저 생성.
  - `super(100)` 실행.
- `super(100)`로 인해 `SuperClass(int i)` 호출
  - `this()`에 의해 다른 생성자 `SuperClass()` 호출.
  - `SuperClass` 클래스의 인스턴스 초기화.
  - `a`가 초기화됨에 따라 **`1번 문장입니다.`** 출력.
  - **`3번 문장입니다.`** 출력.
  - **`4번 문장입니다.`** 출력.
- `Exam01_InheritanceOrder` 클래스의 인스턴스 생성
  - `Exam01_InheritanceOrder` 클래스의 인스턴스 초기화.
  - `c`가 초기화됨에 따라 **`6번 문장입니다.`** 출력.
- **`8번 문장입니다.`** 출력.
- `SuperClass` 클래스의 `myFunc()` 실행
  - **`5번 문장입니다.`** 출력.
- Overriding 된 `myFunc()` 실행
  - **`9번 문장입니다.`** 출력.

> ### Overriding
무슨 타입이든지 간에 메서드가 Overriding이 일어났다면, 가장 최근 Overriding이 된 메서드를 호출한다. 이를 동적 바인딩이라고 한다.
```java
SuperClass obj = new Exam01_InheritanceOrder();               // 9번 문장입니다.
Exam01_InheritanceOrder obj = new Exam01_InheritanceOrder();  // 9번 문장입니다.
```
필드의 경우에는 선언한 타입을 따라간다. 즉 동적 바인딩이 일어나지 않으며 Shadow가 일어난다.

> ### Type Casting
```java
SuperClass obj = new Exam01_InheritanceOrder();
```
`obj`는 `SuperClass` 타입이기 때문에 `c`에 접근이 불가능하다. 이를 가능하게 하려면 Type Casting과 연산자 우선순위를 고려하여 다음과 같이 작성한다.
```java
SuperClass obj = new Exam01_InheritanceOrder();
((Exam01_InheritanceOrder)obj).c = 100;
```

> ### 인스턴스 초기화
```java
public Exam01_InheritanceOrder() {
	super(100);
    staticCall("8번 문장입니다.");
    super.myFunc();
}
```
```java
public SuperClass(int i) {
	this();
    staticCall("4번 문장입니다.");
}
```
생성자를 통한 인스턴스 생성 및 초기화 과정은 `super()`나 `this()` 이후에 수행한다.

## 객체지향

### Abstraction
객체들의 공통적인 특징(속성과 기능)을 추출하여 이름을 붙이는 것을 말한다. 클래스를 정의하는 과정 자체가 추상화이다.

### Encapsulation
데이터 구조와 데이터를 다루는 방법을 결합시켜 묶는 것을 말한다. 클래스로부터 파생된 인스턴스가 독립적으로 역할을 수행하기 위해 필드와 메서드를 묶어서 관리한다.

이를 바탕으로 클래스의 재활용성을 높이고 에러 발생을 최소화한다.

### Information Hiding
클래스는 외부에서 알 필요가 없는 필드나 메서드를 노출시킬 피룡가 없다. 불필요한 정보는 숨기고 최소한의 정보만을 이용하여 클래스를 쉽게 사용할 수 있어야 한다.

Access Modifier를 이용하여 선언된 클래스의 외부 접근을 제어하며 정보를 은폐하는 방식을 이용한다.

### `final` Keyword
클래스 앞에 `final` 키워드가 붙었을 경우 해당 클래스는 상속할 수 없음을 의미한다.

필드 앞에 `final` 키워드가 붙었을 경우 해당 필드를 변경할 수 없음을 의미한다.

메서드 앞에 `final` 키워드가 붙었을 경우 해당 메서드를 하위 클래스에서 Overriding 할 수 없음을 의미한다.

### Abstract Class
Abstract Class는 인스턴스를 생성할 수 없다는 특징이 있다. `abstract` 키워드를 사용한다.

Abstract Class를 상속받은 하위 클래스에서 Abstract Method를 Overriding 한 후 인스턴스를 생성한다.

```java
public abstract class MyClass {
    int kk;

    public MyClass() {
    }

    // 메서드의 정의
    public void printInfo() {
        System.out.println("로직처리를 해요.");
    }

    public abstract void myFunc();
}

class MySubClass extends MyClass {
    @Override
    public void myFunc() {
    }
}
```

### Polymorphism
다형성은 부모의 객체 타입으로 다양한 형태의 자식 객체를 생성할 수 있는 것을 의미한다.

![](https://velog.velcdn.com/images/pyoung/post/1a5dc05d-7e77-49c8-922d-4fa71f6b2139/image.png)

다형성을 이용하여 상위 데이터 타입으로 인스턴스의 타입을 통일해서 사용할 수 있다.

![](https://velog.velcdn.com/images/pyoung/post/cb7c49c7-023c-431c-b400-02f3f75b21c2/image.png)

이를 이용하면 인스턴스들을 상위 데이터 타입으로 통일하면 프로그램의 처리가 쉬워진다.

![](https://velog.velcdn.com/images/pyoung/post/a2d3d2e0-d412-473c-8e82-09dee82bea94/image.png)

### Dynamic Binding
![](https://velog.velcdn.com/images/pyoung/post/4380c5ba-2b74-466e-8b79-755806312c05/image.png)

![](https://velog.velcdn.com/images/pyoung/post/abfdd81f-0fe7-4981-9d08-af40714e63bf/image.png)

`student`와 `teacher`은 `Person` 타입이기 때문에 다음과 같은 경우 에러가 발생한다.
![](https://velog.velcdn.com/images/pyoung/post/e5db2ba5-ac7a-4d65-8d6b-ef9ddc945fae/image.png)

따라서 Type Casting을 통해 메서드를 사용한다.
![](https://velog.velcdn.com/images/pyoung/post/d068a151-adce-4757-b602-177c79762dc8/image.png)

### Interface
인터페이스는 동일한 기능에 대해서 같은 메서드 명을 사용하도록 강제한다. 메서드 명은 지정해 주지만 내부 구현은 각자 클래스에 맞게 구현한다.

인터페이스 내부에 선언하는 필드는 모두 `public`, `static`, `final`이다. 또한 인터페이스 내부에 선언하는 메서드는 모두 Abstract Method이다.

인터페이스를 사용하기 위해서는 구현하고자 하는 클래스에서 `implements` 키워드를 사용한다. 또한 인터페이스는 다중 구현을 허용한다.

```java
public interface MyInterface {
    // Fields
    // 모두 public static final 이다.
    public static final int k = 100;
    String name = "홍길동";

    // Methods
    // 모두 Abstract Method 이다.
    public abstract void printInfo();
    public int myData(int kk);
}

// 인터페이스로부터 직접적인 인스턴스 생성은 불가능하다.
// 인터페이스는 구현을 통해 사용한다.
class MySubClass implements MyInterface {
    @Override
    public void printInfo() {
        // printInfo 내부 구현
    }

    @Override
    public int myData(int kk) {
        // myData 내부 구현
    }
}
```

> 인터페이스는 상속의 개념과 유사하다. 따라서 IS-A 관계도 성립한다.

## Object Class

모든 클래스의 최상위 클래스이다.

### `equals()` Method
```java
public class MyObjectClass {
    String name;

    @Override
    public boolean equals(Object obj) {
        if (!(obj instanceof MyObjectClass)) {
            return false;
        }
        
        MyObjectClass target = (MyObjectClass) obj;
        if (this.name.equals(target.name)) {
            return true;
        }
        return false;
    }

    public static void main(String[] args) {
        MyObjectClass a = new MyObjectClass();
        MyObjectClass b = new MyObjectClass();

        a.name = "홍길동";
        b.name = "홍길동";

        System.out.println(a == b);

        // 클래스의 equals() 메서드는 내부적으로 ==와 동일한 연산 수행
        // equals() 메소드 Overriding
        System.out.println(a.equals(b));
    }
}
```

### `toString()` Method
```java
public class MyStringClass {
    String name;

    public MyStringClass(String name) {
        super();
        this.name = name;
    }

    @Override
    public String toString() {
        return "이름은 " + this.name;
    }

    public static void main(String[] args) {
        MyStringClass a = new MyStringClass("홍길동");
        System.out.println(a);
    }
}
```