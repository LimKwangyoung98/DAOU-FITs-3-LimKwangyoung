# TIL 250228

**다우기술 FITs 3기 임광영**

**학습 날짜 : 2025년 02월 28일 금요일**

---

`StockRecommendPage`에서 주식 추천 데이터를 로딩하는 과정을 개선했다.
`useFetchUserSectorWeight`를 사용하여 데이터를 가져오고, 로딩 상태를 명확하게 분리하여 `LoadingSpinner`를 먼저 표시하도록 만들었다.

이제 데이터가 없는 경우 `RecommendFail`을, 데이터가 있는 경우 `RecommendSuccess`를 반환하도록 조건 분기를 정리했다.
불필요한 중첩 조건을 줄이고 `return`을 분리하여 코드 흐름이 더 직관적이고 가독성이 좋은 구조로 개선되었다.

```jsx
if (loading) {
    return <LoadingSpinner />;
}

if (!recommendFlag || recommendFlag.length === 0) {
    return <RecommendFail />;
}

return <RecommendSuccess data={recommendFlag} />;
```
