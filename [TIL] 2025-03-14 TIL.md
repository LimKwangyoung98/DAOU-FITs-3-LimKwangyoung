# TIL 250314

**다우기술 FITs 3기 임광영**

**학습 날짜 : 2025년 03월 14일 금요일**

---

## 구조체

### 구조체 기본 개념
서로 다른 형태의 데이터를 여러 개 모아서 하나의 자료구조로 표현한 것이다. 구조체로 만들어진 자료는 연속된 메모리에 할당된다.

기본 구조는 다음과 같다.
```c
struct 구조체이름 {
	자료형 식별자1;
    자료형 식별자2;
    ...
};
```

구조체를 사용하기 위해서는 다음과 같이 선언한다.
```c
struct 구조체이름 구조체변수명;
```

구조체 선언 시 변수도 함께 지정할 수 있다.
```c
struct 구조체이름 {
	자료형 식별자1;
    자료형 식별자2;
    ...
} 구조체변수명;
```

`typedef` 키워드를 이용해서도 가능하다. `typedef`는 모든 자료형을 사용자가 지정하는 자료형으로, 가독성과 이식성을 향상시킨다.
```c
typedef struct 구조체이름 {
	자료형 식별자1;
    자료형 식별자2;
    ...
} 자료형명;

int main() {
	자료형명 구조체변수명;
}
```

### `typedef`
`typedef`는 타입 정의를 위한 C 언어 키워드로, 기존의 타입에 대해 새로운 이름을 부여하는 역할이다. 이를 통해 코드의 가독성을 높일 수 있다.
```c
typedef 기존자료형 새로운자료형명
```

이는 복잡한 데이터 타입을 더 간단하고 명료하게 나타낼 수 있다.
```c
typedef unsigned long long ULL;

ULL a = 1000000000000; 
```

이는 함수 포인터나 구조체를 사용할 때 매우 유용하다.
```c
typedef int (*func_ptr)(int, int);
```

이를 바탕으로 특정 데이터 타입을 일관되게 사용할 수 있고, 코드 전체를 수정할 필요가 없다.

> 구조체를 사용할 때는 주로 `typedef`를 사용한다.

### 구조체 초기화
다음과 같이 초기화하며 바로 값을 할당할 수 있다.
```c
struct student {
	char name[20];
    int kor;
};

struct student st1 = { "hee", 100 };
```

> 일반적으로 문자열에는 `strcpy()` 함수를 사용하는 것이 맞다. 하지만 선언하며 할당 시에는 문자 리터럴을 그대로 사용해도 된다.

`typedef`를 활용하여도 동일하다.
```c
typedef struct student {
	char name[20];
    int kor;
} STU;

STU st1 = { "hee", 100 };
```

다음과 같이도 가능하다.
```c
```c
struct student {
	char name[20];
    int kor;
} st1 = { "hee", 100 };
```

선언 후 초기화 할 경우 문자열은 `strcpy()` 함수를 사용한다.
```c
```c
struct student {
	char name[20];
    int kor;
};

struct student st1;
// st1 = { "hee", 100 };  // 불가능
st1 = { strcpy(st1.name, "hee"), 100 };
```