# TIL 250324

**다우기술 FITs 3기 임광영**

**학습 날짜 : 2025년 03월 24일 월요일**

---

## C언어 전처리기

C언어 전처리기는 컴파일 전에 소스 코드에 특정 작업을 수행하는 기능이다. 이를 통해 
코드의 유지보수성을 높이고 중복을 줄이며 효율적인 컴파일을 할 수 있다.

### `#include`
여러 파일에서 공통적으로 사용되는 헤더 파일을 쉽게 포함 할 수 있다. 코드의 재사용과 유지보수성을 향상시킬 수 있으며, 코드가 모듈화되어 유지보수가 쉬워진다.

- 반복되는 코드를 제거할 때 이용
- 함수 및 구조체를 헤더 파일로 분
- 팀 프로젝트시 공통 헤더를 공유

```c
#include <stdio.h>  // 표준 라이브러리 포함
#include “add.h”  // 사용자 정의 헤더 포함
```

### `#define`
상수 및 함수를 간결하게 정의 할 수 있다. 코드 간소화 및 가독성을 향상할 때 이용하며, 상수를 정의하여 의미 있는 코드를 작성할 수 있다. 이를 통해 함수로 반복되는 연산을 줄일 수 있다.

```c
#define PI 3.14159
#define POW(X) ( (X) * (X) )
```

### `#ifdef`, `#ifndef`, `#endif`
디버깅 코드를 관리한다. 특정 조건에 따라 코드 실행을 다르게 설정할 수 있으며, 운영체제 또는 환경에 따른 코드 분기 처리가 가능하다.

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#define DEBUG  // 주석 처리하면 디버깅 로그가 출력되지 않음 // 주석 처리하면 디버깅 로그가 출력되지 않음

void save_trades(const char* filename) {
    FILE* file = fopen(filename, "w");

    #ifdef DEBUG
        printf("[DEBUG] 파일을 열고 있습니다: %s\n", filename);
    #endif
        if (file == NULL) {
            printf("파일을 열 수 없습니다!\n");
            return;
        }
        fprintf(file, "거래 내역: 100,000원 입금\n");
    
    #ifdef DEBUG
        printf("[DEBUG] 파일에 데이터를 기록했습니다.\n");
    #endif
        fclose(file);

    #ifdef DEBUG
        printf("[DEBUG] 파일을 닫았습니다.\n");
    #endif
        printf("거래 내역이 저장되었습니다!\n");
}

int main() {
    save_trades("trades.txt");
    return 0;
}
```

이를 바탕으로 헤더파일이 중복되는 문제를 방지할 수 있으며, 코드 안정성을 확보한다.
```c
#ifndef MYHEADER_H
#define MYHEADER_H

void hello() {
	printf("Hello, World!\n");
}

#endif
```

### `#pragma`
컴파일러를 지시함으로써 특정 컴파일러 설정을 변경할 수 있다. 다음과 같이 특정 경고를 무시할 수 있다.

```c
#pragma warning(disable: 4996)  //특정 경고 비활성화
```

> `#define _CRT_SECURE_NO_WARNINGS`을 사용하지 않아도 된다.


### `#error`
특정 조건을 만족하면 컴파일을 중단하고 오류 메시지를 표시한다. 잘못된 환경에서 빌드를 막으며, 사용자가 특정 설정을 강제하도록 유도한다.

```c
#ifndef SUPPORTED_VERSION
	#error "해당 환경에서 지원되지 않습니다."
#endif
```

다음과 같이 운영체제 검사를 실시할 수 있다.
```c
#include <stdio.h>

// 특정 운영체제(OS) 체크
#ifndef _WIN32
	#error "Windows에서만 컴파일할 수 있습니다."
#endif	

// C99 이상에서만 빌드 가능하도록 강제
#if __STDC_VERSION__ < 199901L
	#error "C99 이상이 필요합니다. 컴파일러를 업그레이드하세요."
#endif

int main() {
	printf("Windows에서 C99 표준으로 컴파일되었습니다.\n");

	return 0;
}
```

## 모듈화

프로그램을 독립적인 기능을 수행하는 작은 단위(모듈)로 나누어 개발하는 소프트웨어 설계 기법이다. 모듈은 각각의 기능을 담당하며 서로 독립적으로 작성, 수정, 테스트할 수 있어 유지보수성과 재사용성이 증가한다.

논리적으로 같은 등급의 소스 파일을 계속해서 분할하는 것이며, 소스파일에서 혹은 어떤 함수에서 일어나는 일의 책임을 그 소스 파일과 그 함수가 지도록 하는 것이다.

### C언어 모듈화
- 헤더파일과 소스파일을 분리하여 모듈화 구현
  - 헤더파일(`.h`) : 함수 선언 및 전역 변수 정의하는 부분.
  - 소스파일(`.c`) : 함수 실제 구현하는 부분
  - 함수선언(인터페이스)과 구현을 나누어 관리.
- 독립적인 코드 관리 기능
  - 필요할 때 필요한 모듈만 수정.
  - 한 모듈을 다른 프로젝트에서도 재사용.
- 컴파일 시간 단축 및 협업 용이
  - 여러 개발자가 각각 모듈을 동시에 개발.
  - 변경된 파일만 다시 컴파일하여 빌드 속도 개선.
  
### 모듈화의 기준
- 하나의 모듈은 한 가지 역할만 수행
- 인터페이스와 구현 분리, 헤더파일과 소스파일 분리
- 높은 응집도와 낮은 결합도, 관련 기능끼리 묶고 모듈 간 의존성을 최소화
- 재사용성 고려 : 공통 기능은 별도의 모듈로 분리
- 변경 가능성 고려 : 확장 가능하도록 설계하여 유지보수 용이

### 모듈화 방법
- 헤더 파일(`.h`)과 소스 파일(`.c`) 분리
  - 헤더 파일 : 함수 선언, 매크로, `typedef`.
  - 소스 파일 : 함수 정의 및 내부 로직 구현.
- 각 모듈마다 역할을 명확히 분리
  - `main.c` : 프로그램 진입점 역할.
  - `bank.h`, `bank.c` : 시스템의 핵심 로직 역할.
  - `db.h`, `db.c` : 데이터베이스 연동 역할.
  - `ui.h`, `ui.c` : 화면에 관련된 역할.
- 헤더 가드 사용하기 
  - `#ifndef` ~ `#define` ~ `#endif`.
- 전역 변수는 `extern`으로 선언하여 여러 파일에서 공유
- `static`을 사용하여 내부에서만 사용되는 함수 숨기기
  - 외부에서 접근 불가능한 변수를 사용해서 독립적으로 모듈화.
  
### 전역 `static` 변수
전역 `static` 변수는 같은 이름의 변수를 다른 파일에서 중복 선언 할 수 있다. `count`, `num`, `data`와 같은 이름은 흔히 여러 함수나 파일에서 사용할 수 있습니다. 이때 `static` 변수로 선언해 주면 동일한 변수명으로의 오류를 줄일 수 있다.

### 소스파일의 분할규칙
- `main`은 되도록 다른 함수, 구조체(정의 포함), 변수를 포함하지 않음
- 각각의 역할 별로 소스파일을 분할
- 대부분 다른 소스파일에서의 전역변수를 직접 끌어 쓰지 않음
- 각 소스파일은 가능한 한 독립적이게 만듦
- 한 소스파일이 다른 소스파일에 의존적이게 되는 경우는 상관없지만 서로 의존되게 만들지는 않음
- 각 소스파일의 전역변수들은 특별한 경우가 아니라면 무조건 `static`