# TIL 250311

**다우기술 FITs 3기 임광영**

**학습 날짜 : 2025년 03월 11일 화요일**

---

## 변수

### 지역 변수 (Local Variable)
중괄호 내부 또는 함수의 매개변수에서 사용되는 변수이다. 지역변수의 지역은 함수의 내부, 중괄호 내부를 말하며 만든 곳을 벗어나면 변수의 메모리 공간이 소멸 된다.

지역변수는 초기화 하지 않으면 컴파일 에러가 나거나 쓰레기값이 저장된다.

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

void counterFunction() {
	static int count = 0;  // 정적 지역변수 (한 번만 초기화됨)
    count++;  // 함수가 호출될 때마다 증가
    
    printf("Count: %d\n", count);
 }
 
int main() {
	counterFunction();  // count = 1
    counterFunction();  // count = 2
    counterFunction();  // count = 3
    
    return 0;
}
```

### 전역 변수 (Global Variable)
중괄호 외부에 선언되는 변수이다. 어느 지역에서든 참조할 수 있으며, 전처리기 밑에 선언하고 반드시 상수로 초기화 해야 한다. 초기값을 지정하지 않으면 기본값이 `0`으로 자동 초기화 된다.

모든 곳에 접근 가능하고 프로그램 종료 전에 메모리가 소멸되지 않는다. 잘못 이용하면 유지보수, 재사용등이 어려워 질 수 있으며, 이를 보완한 것이 정적변수이다.

### 정적 변수 (Static Variable)
프로그램 종료되기 전까지 메모리가 소멸되지 않는 변수이다. 함수를 벗어나도 변수가 사리지지 않고 유지된다.

초기화할 때 반드시 상수로 초기화 해야 하며, 초기화하지 않으면 디폴트값 `0`으로 자동 초기화 된다.

정적변수의 종류로는 다음과 같다.
- 정적 전역변수(static global variable)
  - 자신이 선언된 소스 파일에서만 사용할 수 있고 외부에서는 가져다 쓸 수 없음.
  - 전역변수에 `static`을 붙이면 변수의 범위를 파일 범위로 제한하는 효과를 냄.
- 정적 지역변수(static local variable)
  - 중괄호 내부에서만 사용할 수 있음.
  - 한번 초기화 되면 이후에 함수 호출시 값의 초기화를 무시.

![](https://velog.velcdn.com/images/pyoung/post/1acf4e46-929d-4225-9a39-969efcbf05fd/image.png)

```c
// file1.c

#include <stdio.h>

// 정적 전역변수 (file1.c 내부에서만 접근 가능)
static int count = 0;

void increment() {
	count++;  // 정적 전역변수 값 증가
    
    printf("Count in file1.c: %d\n", count);
}
```
```c
// file2.c

#include <stdio.h>
// extern int count;  // file1.c의 count를 사용하려 하면 오류 발생!

void dummyFunction() {
	// count++;  // 접근 불가능 (컴파일 에러 발생)
    printf("Cannot access count from file2.c\n");
}
```
```c
// main.c

#include <stdio.h>
// file1.c의 함수 사용
void increment();

int main() {
	increment();  // count = 1
    increment();  // count = 2
    
    return 0;
}
```

> 전역변수와 정적변수는 비슷하지만 사용범위(Scope)에서 큰 차이를 갖고 있다.

> Data 영역은 초기화 된 전역변수 및 정적변수가 존재하는 공간이다.
BSS 영역은 초기화 되지 않은 전역변수 및 정적변수가 존재하는 공간으로 모든 값은 `0`으로 초기화된다.
>
수식이나 함수에 의해 초기화 된 변수일 경우 Data가 아닌 BSS 영역으로 들어가게 된다.
>
자세한 내용은 <a href="https://code4human.tistory.com/128" target="_blank">c언어 정적변수, 지역변수, 전역변수 비교 (static, local, global)</a>을 참고한다.

### 외부 변수 (External Variable)
전역변수중 하나로 여러 파일에서 공유할 수 있는 변수이다. `extern` 키워드를 사용하여 다른 파일에서 선언된 변수를 참조할 수 있다.

초기화된 변수는 선언된 파일에서만 메모리를 차지하며 새로운 메모리를 할당받지는 않는다. 다른 파일에서는 `extern`을 통해 참조만 할 수 있다.

> 전역변수는 정적변수와는 다르게 `extern` 키워드를 통해 참조할 수 있으며, 보안성이 상대적으로 낮다.

```c
// file1.c

#include <stdio.h>

int globalVar = 10;  // 외부 변수 (전역 변수)

void printGlobalVar() {
	printf("globalVar in file1.c: %d\n", globalVar);
}
```
```c
// file2.c

#include <stdio.h>
	extern int globalVar;  // 외부 변수 선언 (실제 메모리 할당 X)
    
void modifyGlobalVar() {
	globalVar += 5;  // 값 변경
    
    printf("globalVar in file2.c after modification: %d\n", globalVar);
}
```
```c
// main.c

#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

void printGlobalVar();
void modifyGlobalVar();

int main() {
	printGlobalVar();      
	modifyGlobalVar();     
	printGlobalVar();      
    
return 0;
```

### 레지스터 변수 (Register Variable)
CPU 레지스터에 저장될 가능성이 있는 변수이다. 목적은 자주 사용하는 변수를 메모리가 아니라 레지스터에 저장하여 접근 속도를 높이는 것이다. 포인터로 접근할 수는 없다.

컴파일러가 `register`라고 쓰면 반드시 레지스터에 저장하는 것은 아니며, 단순한 제안일 뿐
이다. 현재 컴파일러는 최적화를 자동으로 수행하므로 `register` 키워드는 거의 사용하지 않는다.

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

void registerExample() {
	register int i;  // 레지스터 변수 선언
    for (i = 0; i < 5; i++) {
    	printf("i = %d\n", i);
	}
}

int main() {
	registerExample();
    
    return 0;
}
```

## 포인터

변수에 저장된 값을 출력하는 경우 실제로 컴퓨터에서는 다음과 같이 동작한다.

1. 기계어로 번역되어 실행파일(`exe`)로 저장
2. 로더에 의해 실행 파일이 메모리에 적재되며 실행
3. 모든 변수는 컴파일러에 의해 생성되는 심벌테이블에 저장되고, 심벌테이블에는 변수명과 메모리 주소가 저장
4. 프로그램이 실행될 때 모든 변수는 심벌 테이블에 있는 주소에 의해 접근

### 기본 개념
포인터는 변수의 메모리 주소를 저장하는 변수이다.
```c
#include <stdio.h>

int main() {
	int a = 10;
    int *p = &a;  // a의 주소를 저장하는 포인터 변수
    
    printf("변수 a의 값: %d\n", a);  // 10
    printf("변수 a의 주소: %p\n", &a);
    printf("포인터 p가 가리키는 값: %d\n", *p);  // 포인터 역참조
    printf("포인터 p가 가리키는 주소: %p\n", p);
    
    return 0;
}
```
```
변수 a의 값: 10
변수 a의 주소: 000000FDEAFDF654
포인터 p가 가리키는 값: 10
포인터 p가 가리키는 주소: 000000FDEAFDF654
```
`*p`를 사용하면 `p`가 가리키는 주소에 저장된 값을 가져올 수 있으며, `&a`를 사용하면 변수 `a`의 메모리 주소를 가져올 수 있다.

> 자료형과 상관없이 주소의 크기는 동일하다.

### 포인터와 배열
배열의 이름 자체가 첫 번째 원소의 주소를 나타내며, 배열의 모든 요소가 메모리의 연속된 기억 공간에 저장된다. 따라서 배열을 제어할 때는 첨자보다는 포인터를 사용하는 것이 더 효율적이다.

> 배열의 이름을 나타내는 변수는 메모리에 저장되지 않지만, 별도의 포인터 변수를 선언하여 사용한다면 메모리에 저장된다.

```c
#include <stdio.h>

int main() {
	int arr[3] = { 1, 2, 3 };
    int* p = arr;  // 배열 이름은 첫 번째 원소의 주소
    
    printf("arr[0] = %d, *p = %d\n", arr[0], *p);
    printf("arr[1] = %d, *(p+1) = %d\n", arr[1], *(p + 1));
    printf("arr[2] = %d, *(p+2) = %d\n", arr[2], *(p + 2));
    
    return 0;
}
```
```
arr[0] = 1, *p = 1
arr[1] = 2, *(p+1) = 2
arr[2] = 3, *(p+2) = 3
```
 
### 이중 포인터
이중 포인터는 포인터를 가리키는 포인터다. 메모리 주소를 저장하는 포인터 변수의 주소를 저장하는 변수이다.
```c
#include <stdio.h>

int main() {
	int a = 10;
	int* p = &a;
	int** pp = &p;  // p의 주소를 저장하는 이중 포인터

	printf("a의 값: %d\n", a);
	printf("p가 가리키는 값: %d\n", *p);
	printf("pp가 가리키는 값(포인터 p의 주소): %p\n", pp);
	printf("pp가 가리키는 주소의 값(= *pp, 즉 a의 값): %d\n", **pp);

	return 0;
}
```
```
a의 값: 10
p가 가리키는 값: 10
pp가 가리키는 값(포인터 p의 주소): 000000A18554F638
pp가 가리키는 주소의 값(= *pp, 즉 a의 값): 10
```

값을 가져오기 때문에 메모리에 여러 번 접근해야 하므로, 일반 변수보다 효율성이 떨어진다. 그럼에도 이중 포인터를 사용하는 이유는 다음과 같다.
- 배열과 문자열의 조작에 포인터를 사용함으로써 간결하고 효율적인 처리
- 배열, 구조체, 공용체와 함수에 포인터를 사용하여 쉽게 접근
- 동적 자료의 처리, 순환 자료 구조의 표현, 참조에 의한 호출 등에 활용