# TIL 250327

**다우기술 FITs 3기 임광영**

**학습 날짜 : 2025년 03월 27일 목요일**

---

FDS 함수의 반환값은 다음과 같이 정의한다.

1. `0` : 이상 거래
2. `1` : 정상 거래

## 1. `int detect_stock_amount(StockOrder*);`

**주문 → 해당 종목의 당일 거래량 비정상적 → 해당 종목 거래 중지**

주문 `StockOrder`이 입력되면 해당 종목의 거래량을 조사한다.

해당 종목의 과거 `month`일 동안의 거래량을 바탕으로 Z-Score을 계산하고, 당일 총 거래량과 비교하여 Z-Score 값이 `stock_threshold` 이상이라면 이상 거래로 판단한다. 해당 종목의 거래를 중지한다.

- `month` : `30`
- `stock_threshold` : `0.674`

## 2. `int detect_account_amount(StockOrder*);`

**주문 → 해당 계좌의 당일 거래량 비정상적 → 해당 계좌 거래 중지**

주문 `StockOrder`이 입력되면 해당 계좌의 거래량을 조사한다.

해당 계좌의 당일 거래량을 바탕으로, 해당 종목의 당일 총 거래량과 비교하여 `ratio`% 이상이라면 이상 거래로 판단한다. 해당 계좌의 거래를 중지한다.

- `ratio` : `30`

## 3. `int detect_wash_sale(StockOrder*);`

**주문 → 해당 계좌의 주문 빈도 비정상적 → 해당 계좌 거래 중지**

주문 `StockOrder`이 입력되면 해당 계좌의 주문 빈도를 조사한다.

해당 계좌의 주문 일자를 바탕으로, `time_interval`까지의 주문 횟수를 구하고 `lambda` 값을 바탕으로 Z-Score를 계산한다. 해당 값이 `lambda_threshold` 이상이라면 이상 거래로 판단한다. 해당 계좌의 거래를 중지한다.

- `time_interval` : `300`
- `lambda` : `1`
- `lambda_threshold` : `1.645`