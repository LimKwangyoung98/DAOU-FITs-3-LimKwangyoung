# TIL 250116

**다우기술 FITs 3기 임광영**

**학습 날짜 : 2025년 01월 16일 목요일**

---

## `JOIN`

### `JOIN`
여러 테이블의 데이터를 결합하여 하나의 결과 집합을 생성하는 SQL 절이다. 기본적으로 두 개 이상의 테이블을 연결하며, 테이블 간 관계를 활용하여 데이터를 조회한다.

`JOIN`의 필요성은 다음과 같다.
- 데이터의 일관성과 중복 방지를 위해 데이터를 여러 테이블로 나누어 저장(정규화).
- 나누어진 데이터를 조합하여 원하는 정보를 조회할 때 `JOIN` 사용.

### `INNER JOIN`
두 테이블 간에 일치하는 데이터만 반환한다. 기본적인 `JOIN` 방식이다.
```sql
SELECT E.NAME, D.DEPARTMENT_NAME
FROM EMPLOYEES E
INNER JOIN DEPARTMENTS D
  ON E.DEPARTMENT_ID = D.DEPARTMENT_ID;
  
SELECT E.NAME, D.DEPARTMENT_NAME
FROM EMPLOYEES E, DEPARTMENTS D
WHERE E.DEPARTMENT_ID = D.DEPARTMENT_ID;
```

### `LEFT OUTER JOIN`
LEFT 테이블의 모든 행과 일치하는 RIGHT 테이블의 데이터를 반환한다. 일치하지 않을 경우 `NULL`을 반환한다.
```sql
SELECT E.NAME, D.DEPARTMENT_NAME
FROM EMPLOYEES E
LEFT OUTER JOIN DEPARTMENTS D
  ON E.DEPARTMENT_ID = D.DEPARTMENT_ID;
```

### `RIGHT OUTER JOIN`
RIGHT 테이블의 모든 행과 일치하는 LEFT 테이블의 데이터를 반환한다. 일치하지 않을 경우 `NULL`을 반환한다.
```sql
SELECT E.NAME, D.DEPARTMENT_NAME
FROM EMPLOYEES E
RIGHT OUTER JOIN DEPARTMENTS D
  ON E.DEPARTMENT_ID = D.DEPARTMENT_ID;
```

### `FULL OUTER JOIN`
LEFT 테이블과 RIGHT 테이블의 모든 행을 반환한다. 일치하지 않는 데이터는 `NULL`을 반환한다.
```sql
SELECT E.NAME, D.DEPARTMENT_NAME
FROM EMPLOYEES E
FULL OUTER JOIN DEPARTMENTS D
  ON E.DEPARTMENT_ID = D.DEPARTMENT_ID;
```

### `SELF JOIN`
같은 테이블을 두 번 사용하여 `JOIN`을 수행한다. 주로 계층 구조를 표현할 때 사용한다.
```sql
SELECT E1.NAME AS EMPLOYEE, E2.NAME AS MANAGER
FROM EMPLOYEES E1
JOIN EMPLOYEES E2
  ON E1.MANAGER_ID = E2.EMPLOYEE_ID;  --E1: 자식 테이블, E2: 부모 테이블
```

사원코드 `EMPLOYEE_ID`와 사수코드 `MANAGER_ID`를 `ON`으로 연결한다. `E1`의 사원코드가 `E2`의 사수코드와 일치하는지 확인하기 때문에 `E1`은 자식 테이블, `E2`는 부모 테이블이다. 결과는 다음과 같다.
![](https://velog.velcdn.com/images/pyoung/post/5e9f1c9e-f18c-4bd2-b4b6-a5d6d56e2b38/image.png)

> 같은 테이블을 여러 번 조회하는 것보다 계층 쿼리를 활용하는 것이 더 적절하다.

### `CROSS JOIN`
두 테이블 간의 모든 행의 조합을 반환(Cartesian Product)하며, `JOIN` 조건이 없는 경우 발생한다.
```sql
SELECT E.NAME, D.DEPARTMENT_NAME
FROM EMPLOYEES E
CROSS JOIN DEPARTMENTS D;
```

`E` 테이블의 크기가 $M$이고 `D` 테이블의 크기가 $N$일 때, `CROSS JOIN`한 테이블의 크기는 $M \times N$이다.

### `NATURAL JOIN`
동일한 이름의 컬럼을 기준으로 `JOIN`한다. 조건을 명시하지 않아도 자동으로 연결한다.
```sql
SELECT NAME, DEPARTMENT_NAME
FROM EMPLOYEES
NATURAL JOIN DEPARTMENTS;
```

> `EMPLOYEES` 테이블과 `DEPARTMENTS` 테이블은 `DEPARTMENT_ID`와 `MANAGER_ID`를 동일한 컬럼으로 갖는다. 이때 `SELECT`하는 컬럼 리스트를 `EMPLOYEES.DEPARTMENT_ID`와 같이 테이블을 명시하지 않는다.
```sql
```sql
SELECT NAME, DEPARTMENT_NAME, DEPARTMENT_ID, MANAGER_ID
FROM EMPLOYEES
NATURAL JOIN DEPARTMENTS;
```

> 현업에서 `NATURAL JOIN`을 자주 사용하지는 않는다.

### `JOIN` 사용 시 유의사항
- `JOIN` 조건 누락
  - `JOIN` 조건이 없으면 `CROSS JOIN`과 동일한 결과가 발생.
- `NULL` 처리
  - `OUTER JOIN` 시 `NULL` 값을 처리하는 로직 필요.
- 성능
  - 큰 테이블 간 `JOIN`은 성능 저하가 가능하며, 적절한 인덱스 설정 필요.

### `JOIN` 예제
```sql
--고객 이름과 그 고객이 보유한 계좌 유형을 조회
SELECT C.NAME, A.ACCOUNT_TYPE
FROM CUSTOMERS C
INNER JOIN ACCOUNTS A
  ON C.CUSTOMER_ID = A.CUSTOMER_ID;

SELECT C.NAME, A.ACCOUNT_TYPE
FROM CUSTOMERS C, ACCOUNTS A
WHERE C.CUSTOMER_ID = A.CUSTOMER_ID;

--고객 이름과 그 고객이 수행한 거래 금액
SELECT C.NAME, T.AMOUNT
FROM CUSTOMERS C
INNER JOIN ACCOUNTS A
  ON C.CUSTOMER_ID = A.CUSTOMER_ID
INNER JOIN TRANSACTIONS T
  ON A.ACCOUNT_ID = T.ACCOUNT_ID;

--모든 고객의 이름과 각 고객이 보유한 계좌 유형을 조회
SELECT C.NAME, A.ACCOUNT_TYPE
FROM CUSTOMERS C
LEFT OUTER JOIN ACCOUNTS A
  ON C.CUSTOMER_ID = A.CUSTOMER_ID;

--모든 고객과 지점을 조합하여 조회
SELECT C.NAME CUSTOMER_NAME, B.NAME BRANCH_NAME
FROM CUSTOMERS C
CROSS JOIN BRANCHES B;
```

> `CROSS JOIN`과 `NATURAL JOIN`은 테이블 간의 참조 관계가 필수적이지 않기 때문에 `ON` 키워드를 사용하지 않는다.

> ### Oracle `JOIN`
`OUTER JOIN`을 다음과 같이 표현할 수도 있다.
```sql
SELECT E.EMPLOYEE_ID, E.NAME EMPLOYEE_NAME, D.DEPARTMENT_NAME, D.LOCATION
FROM EMPLOYEES E
LEFT OUTER JOIN DEPARTMENTS D
  ON E.DEPARTMENT_ID = D.DEPARTMENT_ID;
>
SELECT E.EMPLOYEE_ID, E.NAME EMPLOYEE_NAME, D.DEPARTMENT_NAME, D.LOCATION
FROM EMPLOYEES E, DEPARTMENTS D
WHERE E.DEPARTMENT_ID = D.DEPARTMENT_ID (+);
```

### 심화 문제
```sql
--3. 직원과 급여 정보 조회
--직원 이름과 급여를 조회하되 급여가 NULL인 경우 N/A로 출력하세요.  
SELECT NAME, NVL(TO_CHAR(SALARY), 'N/A') AS SALARY
FROM EMPLOYEES;
```
`NVL` 함수의 경우 두 인자의 자료형이 동일해야 한다. 따라서 `SALARY` 컬럼을 `'N/A'`와 동일하게 문자형으로 변환해준다.

```sql
--6. 최근 3개월 내 입사한 직원
--최근 3개월 이내 입사한 직원 이름과 입사일을 조회하시오.
SELECT NAME, HIRE_DATE
FROM EMPLOYEES
WHERE MONTHS_BETWEEN(SYSDATE, HIRE_DATE) <= 3;
```
개월을 계산할 때는 `MONTHS_BETWEEN` 함수를 사용한다. 이 외에도 `ADD_MONTHS`나 `INTERVAL`을 사용하는 방법도 있다.

```sql
--12. 대출 상태별 고객 수
--대출 상태별 고객 수를 조회하세요.
SELECT STATUS, COUNT(DISTINCT CUSTOMER_ID) COUNT
FROM LOANS
GROUP BY STATUS;
```
한 고객이 여러 번 대출을 할 수도 있기 때문에 `DISTINCT` 키워드를 사용한다.

```sql
--14. 월별 신규 고객 수
--월별 신규 고객 수를 최근순으로 조회하세요.
SELECT TO_CHAR(CREATED_AT, 'YYYY-MM') MONTH, COUNT(*) COUNT
FROM CUSTOMERS
GROUP BY TO_CHAR(CREATED_AT, 'YYYY-MM')
ORDER BY MONTH DESC;
```
날짜에서 월을 추출할 때는 `TO_CHAR` 함수를 사용한다. 이를 통해 쉽게 정렬할 수 있다.

```sql
--18. 고객별 거래 내역
--고객 이름과 총 거래 내역(금액)을 조회하여 고객 이름을 오름차순 하세요.
SELECT C.NAME, NVL(SUM(T.AMOUNT), 0) TOTAL_AMOUNT
FROM CUSTOMERS C
LEFT OUTER JOIN ACCOUNTS A
  ON C.CUSTOMER_ID = A.CUSTOMER_ID
LEFT OUTER JOIN TRANSACTIONS T
  ON A.ACCOUNT_ID = T.ACCOUNT_ID
GROUP BY C.NAME
ORDER BY TO_NUMBER(SUBSTR(C.NAME, 10, LENGTH(C.NAME)));
```
고객 이름을 기준으로 정렬할 시 `ORDER BY C.NAME`을 하면 고객 번호가 숫자가 아닌 문자를 기준으로 정렬된다. 따라서 `SUBSTR` 함수를 통해 숫자 부분을 추출하고 `TO_NUMBER` 함수를 통해 숫자로 변환한 뒤 정렬한다.