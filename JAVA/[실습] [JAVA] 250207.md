# [JAVA] 250207 실습

### 문제 1번
```java
package javaexam2;

import java.util.StringTokenizer;

public class Test03 {
    public static void main(String[] args) {
        String str = "1.22,4.12,5.93,8.71,9.34";

        double[] data = new double[5];
        double sum = 0;

        StringTokenizer st = new StringTokenizer(str, ",");

        for (int i=0; st.hasMoreElements(); i++) {
            data[i] = Double.parseDouble(st.nextToken());
            sum += data[i];
        }

//        int idx = 0;
//        while (st.hasMoreTokens()) {
//            data[idx] = Double.parseDouble(st.nextToken());
//            sum += data[idx];
//            idx++;
//        }

        System.out.printf("합계: %.3f\n", sum);
        System.out.printf("평균: %.3f", sum / data.length);
    }
}
```

### 문제 2번
```java
package javaexam2;

import java.util.*;

public class Test04 {
    public static void main(String[] args) {
        List<Integer> list = new ArrayList<Integer>();

        Random random = new Random();
        for (int i=0; i<10; i++) {
            int randomValue = random.nextInt(10) + 1;
            list.add(randomValue);
            System.out.print(randomValue + " ");
        }
        System.out.println();

        Collections.sort(list);

        for (int ele : list) {
            System.out.print(ele + " ");
        }
    }
}
```

### 문제 3번
```java
package javaexam2;

import java.util.HashMap;
import java.util.Map;
import java.util.Random;

public class Test05 {
    public static void main(String[] args) {
        Map<Integer, Integer> map = new HashMap<Integer, Integer>();
        double sum = 0.0;

        Random random = new Random();
        for (int i=1; i<11; i++) {
            map.put(i, random.nextInt(100) + 1);
        }

        for (Integer key : map.keySet()) {
            System.out.print(map.get(key) + " ");
        }
        System.out.println();

        for (Integer key : map.keySet()) {
            sum += map.get(key);
        }

        System.out.printf("합계: %.2f\n", sum);
        System.out.printf("평균: %.2f", sum / map.size());
    }
}
```

### 문제 4번
```java
// Plane.java

package javaexam2.plane;

abstract class Plane {
    private String planeName;
    private int fuelSize;

    public Plane() {
    }

    public Plane(String planeName, int fuelSize) {
        this.planeName = planeName;
        this.fuelSize = fuelSize;
    }

    public void refuel(int fuel) {
        this.fuelSize += fuel;
    }

    public abstract void flight(int distance);

    public String getPlaneName() {
        return this.planeName;
    }

    public int getFuelSize() {
        return this.fuelSize;
    }

    public void setPlaneName(String planeName) {
        this.planeName = planeName;
    }

    public void setFuelSize(int fuelSize) {
        this.fuelSize = fuelSize;
    }
}
```
```java
// Airplane.java

package javaexam2.plane;

class Airplane extends Plane {
    public Airplane() {
    }

    public Airplane(String planeName, int fuelSize) {
        super(planeName, fuelSize);
    }

    @Override
    public void flight(int distance) {
        int newFuelSize = getFuelSize() - (distance / 10) * 30 ;
        setFuelSize(newFuelSize);
    }
}
```
```java
package javaexam2.plane;

class Cargoplane extends Plane {
    public Cargoplane() {
    }

    public Cargoplane(String planeName, int fuelSize) {
        super(planeName, fuelSize);
    }

    @Override
    public void flight(int distance) {
        int newFuelSize = getFuelSize() - (distance / 10) * 50;
        setFuelSize(newFuelSize);
    }
}
```
```java
// PlaneTest.java

package javaexam2.plane;

public class PlaneTest {
    public static void main(String[] args) {
        Airplane airplane = new Airplane("L474", 1000);
        Cargoplane cargoplane = new Cargoplane("C40", 1000);

        System.out.println("Plane\t\t\tFuelSize");
        System.out.println("------------------------");
        System.out.println(airplane.getPlaneName() + "\t\t\t" + airplane.getFuelSize());
        System.out.println(cargoplane.getPlaneName() + "\t\t\t\t" + cargoplane.getFuelSize());
        System.out.println();

        System.out.println("100 운항");
        airplane.flight(100);
        cargoplane.flight(100);
        System.out.println("Plane\t\t\tFuelSize");
        System.out.println("------------------------");
        System.out.println(airplane.getPlaneName() + "\t\t\t" + airplane.getFuelSize());
        System.out.println(cargoplane.getPlaneName() + "\t\t\t\t" + cargoplane.getFuelSize());
        System.out.println();

        System.out.println("200 주유");
        airplane.refuel(200);
        cargoplane.refuel(200);
        System.out.println("Plane\t\t\tFuelSize");
        System.out.println("------------------------");
        System.out.println(airplane.getPlaneName() + "\t\t\t" + airplane.getFuelSize());
        System.out.println(cargoplane.getPlaneName() + "\t\t\t\t" + cargoplane.getFuelSize());
        System.out.println();
    }
}
```

### 문제 5번
```java
// Employee.java

package javaexam2.company;

abstract class Employee {
    private String name;
    private int number;
    private String department;
    private int salary;

    public Employee() {
    }

    public Employee(String name, int number, String department, int salary) {
        this.name = name;
        this.number = number;
        this.department = department;
        this.salary = salary;
    }

    public abstract double tax();

    public String getName() {
        return name;
    }

    public void setName(String name) {
        this.name = name;
    }

    public int getNumber() {
        return number;
    }

    public void setNumber(int number) {
        this.number = number;
    }

    public String getDepartment() {
        return department;
    }

    public void setDepartment(String department) {
        this.department = department;
    }

    public int getSalary() {
        return salary;
    }

    public void setSalary(int salary) {
        this.salary = salary;
    }

    @Override
    public String toString() {
        return name + "\t\t" + department + "\t\t" + salary;
    }
}
```
```java
// Secretary.java

package javaexam2.company;

class Secretary extends Employee implements Bonus {
    public Secretary() {
    }

    public Secretary(String name, int number, String department, int salary) {
        super(name, number, department, salary);
    }

    @Override
    public double tax() {
        return getSalary() * 0.1;
    }

    @Override
    public void incentive(int pay) {
        int newSalary = getSalary() + (int)(pay * 0.8);
        setSalary(newSalary);
    }
}
```
```java
// Sales.java

package javaexam2.company;

class Sales extends Employee implements Bonus {
    public Sales() {
    }

    public Sales(String name, int number, String department, int salary) {
        super(name, number, department, salary);
    }

    @Override
    public double tax() {
        return getSalary() * 0.13;
    }

    @Override
    public void incentive(int pay) {
        int newSalary = getSalary() + (int)(pay * 1.2);
        setSalary(newSalary);
    }
}
```
```java
// Bonus.java

package javaexam2.company;

interface Bonus {
    public void incentive(int pay);
}
```
```java
// Company.java

package javaexam2.company;

import java.util.HashMap;
import java.util.Map;

public class Company {
    public static void main(String[] args) {
        Map<Integer, Employee> map = new HashMap<Integer, Employee>();

        Employee secretary = new Secretary("Hilery", 1, "secretary", 800);
        Employee sales =  new Sales("Clinten", 2, "sales", 1200);
        map.put(secretary.getNumber(), secretary);
        map.put(sales.getNumber(), sales);

        System.out.println("name\t\tdepartment\t\tsalary");
        System.out.println("----------------------------------");
        for (Integer key : map.keySet()) {
            Employee element = map.get(key);
            System.out.println(element);
        }
        System.out.println();

        System.out.println("인센티브 100 지급");
        for (Integer key : map.keySet()) {
            Employee element = map.get(key);
            if (element instanceof Secretary) {
                Secretary s = (Secretary) element;
                s.incentive(100);
                map.put(s.getNumber(), (Employee) s);
            } else {
                Sales s = (Sales) element;
                s.incentive(100);
                map.put(s.getNumber(), (Employee) s);
            }
        }

        System.out.println("name\t\tdepartment\t\tsalary\t\ttax");
        System.out.println("-------------------------------------------");
        for (Integer key : map.keySet()) {
            Employee element = map.get(key);
            System.out.println(element + "\t\t" + element.tax());
        }
    }
}
```