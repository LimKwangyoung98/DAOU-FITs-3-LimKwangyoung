# TIL 250205

**다우기술 FITs 3기 임광영**

**학습 날짜 : 2025년 02월 05일 수요일**

---

## 객체지향

### Class
![](https://velog.velcdn.com/images/pyoung/post/695322bd-9fd7-43dc-b8a6-ca17dab4b2b0/image.png)

클래스는 객체 모델링의 수단이며 이를 통해 생성한 객체를 인스턴스(Instance)라고 한다. 객체 모델링은 대표적으로 필드(Field)와 생성자(Constructor), 메서드(Method)로 구성된다.

클래스는 또한 ADT(Abstract Data Type)으로도 볼 수 있다. ADT는 사용자 정의 데이터 타입으로, 기존 데이터 타입을 이용해 새로운 데이터 타입을 만드는 것을 의미한다.

### Instance
클래스에서 정의된 필드와 메서드를 사용하기 위해서 Heap 영역에 일정 공간을 확보해야 하며, 이를 인스턴스(Instance) 또는 객체(Object)라고 한다.

Java 프로그래밍에서는 클래스에서 파생된 인스턴스를 바탕으로 동작한다.

### Constructor
Java의 모든 클래스는 적어도 하나 이상의 생성자(Constructor)를 가진다. 반환 타입이 존재하지 않는 메서드 형태이며, 생성자의 이름은 클래스의 이름과 같아야 한다. 만약 생성자를 명시적으로 작성하지 않았다면 자동으로 기본 생성자(Default Constructor)가 추가된다.

인스턴스가 생성될 때 `new` 키워드를 통해 호출되며, 필드에 대한 초기화 등의 작업을 주로 한다.

### JVM 메모리 구조
JVM은 용도에 따라 메모리를 다음과 같이 나누어 관리한다.
- Register
- Runtime Constant Pool
  - 속도와 효율을 위한 고정값(상수) 저장.
- Method Area
  - 클래스를 최초 실행했을 때 이에 대한 정보 저장.
  - Class Variable(`static` 키워드를 활용한 변수)의 공간 저장.
  - 메서드의 Bytecode(실행코드).
- Heap
  - `new` 키워드에 의해 생성된 인스턴트 저장.
- Call Stack 또는 Stack
  - 메서드와 생성자를 위한 공간 저장.
  
### Class Member와 Instance Member
Class Member는 `static` 키워드로 선언한다. Class Member는 모든 인스턴스가 공유하며, 클래스가 메모리에 로드될 때 Method Area에 단 한 번 Class Member을 위한 메모리가 할당되고 초기화된다. 따라서 인스턴스를 생성하지 않아도 사용할 수 있다.

> `static` 키워드의 남용은 재사용성과 유지보수에 악영향을 끼친다.

Instance Member는 각 인스턴스가 독립적으로 메모리 공간을 확보하는 필드와 메서드로, 인스턴스를 이용해야 사용이 가능하다.

```java
public class InstanceTest {
    // 인스턴스 생성 시 Heap 영역에 저장
    int a = 100;

    // Method Area 영역에 저장
    static int b = 100;
    
    public InstanceTest() {
    }

    public static void main(String[] args) {
        InstanceTest d1 = new InstanceTest();
        InstanceTest d2 = new InstanceTest();
    }
}
```
다음의 코드에서 Instance Member는 각 인스턴스마다 독립적인 공간을 확보하기 때문에 `d1.a != d2.a`이다. Class Member는 모든 인스턴스가 공유하기 때문에 `d1.b == d2.b == InstanceTest.b`이다.

### Method Overloading
같은 이름의 메서드가 여러 개 존재하는 것을 의미한다. 메서드 이름이 같더라도 매개변수의 개수와 데이터 타입을 이용하여 메서드를 구분하는 개념이다.

생성자에도 마찬가지로 적용된다.

### Package와 Import
Package는 코드를 효율적으로 관리하고 재사용하는 방법이다. Java의 모든 클래스는 특정 Package에 반드시 포함되어야 하며, 지정하지 않으면 Default Package에 포함되게 된다. Package 명은 폴더명과 같아야한다.

다른 클래스를 사용할 때는 해당 클래스의 Package를 명시해야 하며, 이때 `import` 문을 활용한다.

### Access Modifier
- `public`
  - 어디에서나 접근 가능한 필드, 메서드, 생성자, 클래스 선언에 사용.
  - 파일 하나당 `public`은 한 개를 초과하지 않음.
  - 컴파일했을 때 클래스 단위로(`.class파일`)로 생성.
- `protected`
  - 기본적으로 `default`와 같지만 상속 관계에 있는 클래스에서 필드, 메서드, 생성자를 사용할 수 있도록 선언.
- `default`
  - 같은 Package 내의 다른 class에서 접근 가능한 필드, 메서드, 생성자, 클래스 선언에 사용.
- `private`
  - 다른 클래스에서는 접근할 수 없는 필드, 메서드, 생성자 선언에 사용.
  
![](https://velog.velcdn.com/images/pyoung/post/af1618e2-dfe1-4bcd-87c7-1df3ea3a52c7/image.png)

  
예시 파일의 계층 구조는 다음과 같다.
![](https://velog.velcdn.com/images/pyoung/post/74948d92-b410-414c-8aa4-d95f56e56152/image.png)

```java
// MainClass.java

package MainPackage;

// import MyPackage.MyClass;
import MyPackage.*;

public class MainClass {
    // instance variable
    int k;

    // default constructor
    public MainClass() {
    }

    public static void main(String[] args) {
        MyClass a = new MyClass();
        a.balance = 200;
    }
}
```
```java
// MyClass.java

package MyPackage;

// 클래스 앞에는 Access Modifier가 두 가지 중 하나만 올 수 있다.
// public : Package에 상관없이 어디서나 접근 가능.
// default : 같은 Package 내에서만 접근 가능.
public class MyClass {
    // instance variable
    public int balance;

    // default constructor
    public MyClass() {
    }

    // method
    public void printInfo() {
    }
}
```	
  
모든 필드와 메서드, 생성자, 클래스는 Access Modifier가 사용된다.
만약 클래스가 `public`이어도 내부 변수와 생성자를 외부 Package에서 사용할 수 있는 것은 아니다.
따라서 외부 패키지에서 사용하기 위해서는 클래스 뿐만 아니라 내부 요소도 `public`으로 설정해줘야 한다.

하지만 일반적으로 필드(데이터)를 외부에서 직접적으로 수정하면 안되기 때문에 `private`으로 설정한다. 이를 우회적으로 수정할 수 있는 것이 `setter`와 `getter`이다.
```java
// MainClass.java

package MainPackage;

// import MyPackage.MyClass;
import MyPackage.*;

public class MainClass {
    // instance variable
    int k;

    // default constructor
    public MainClass() {
    }

    public static void main(String[] args) {
        MyClass a = new MyClass();
        // a.balance = 200;  // private이기 때문에 접근 불가
        a.setBalance(200);
    }
}
```
```java
// MyClass.java

package MyPackage;

// 클래스 앞에는 Access Modifier가 두 가지 중 하나만 올 수 있다.
// public : Package에 상관없이 어디서나 접근 가능.
// default : 같은 Package 내에서만 접근 가능.
public class MyClass {
    // instance variable
    private int balance;

    // default constructor
    public MyClass() {
    }

    // getter
    public int getBalance() {
        return this.balance;
    }

    // setter
    public void setBalance(int balance) {
        this.balance = balance;
    }

    // method
    public void printInfo() {
    }
}
```

> 생성자는 일반적으로 `public`으로 설정하지만, 싱글톤(Singleton)에서는 `private`로 설정한다.

> ### `System.out.println();`
`System`은 클래스이며 `java.lang` Package에 속해있다. 따라서 이를 사용하기 위해서는 `import java.lang.*` 문을 작성해야 하지만, Java에서는 이를 기본적으로 제공한다.
>
> `out`은 괄호 `()`가 없기 때문에 메서드가 아닌 `static` 필드(클래스 변수)이다.

### Static Block
클래스에 대한 정보가 Method Area에 로드된 후 Main Thread에 의해서 `main()` 메서드가 호츨되기 이전에 실행된다.
```java
package MainPackage;

public class StaticBlock {
    // Fields
    int k;

    // Constructor

    // Static Block
    static {
        System.out.println("Static Block");
    }

    // Methods
    public static void main(String[] args) {
        System.out.println("Main");
    }
}
```

### Inheritance
상속은 부모 클래스가 가진 내용을 자식 클래스가 물려받는 것이다.

Java에서의 상속은 다음과 같은 특징을 갖는다.
- Java는 다중 상속이 아닌 단일 상속만 지원
- 클래스들은 결국 상속을 이어가며 확장되어 Tree 구조를 구성
- Java의 모든 클래스는 상속 관계에 있음
  - 특정 클래스를 상속하지 않으면 `java.lang.Object` 클래스 상속.
  - 작성하지 않아도 Java가 자동으로 상속.
- 자식 클래스 선언 시 `extends` 키워드를 통해 부모 클래스를 상속

![](https://velog.velcdn.com/images/pyoung/post/9364fe5a-68b3-4eb6-b423-94a4835e074f/image.png)

```java
// Person.java

// 상위 클래스
public class Person {
    // Fields
    String name;
    String phone;
    String address;  // 해당 필드 추가 시 하위 클래스에도 자동 추가

    // Constructor
    public Person() {}

    // Methods
    public void sleep() {
    }

    public void eat() {
    }
}
```
```java
// Student.java

public class Student extends Person {
    // Fields
    String dept;

    // Constructor
    public Student() {}

    // Methods
    public void study() {
    }
}
```
```java
// Teacher.java

public class Teacher extends Person {
    // Fields
    String subject;

    // Constructor
    public Teacher() {}

    // Methods
    public void teach() {
    }
}
```