# TIL 250312

**다우기술 FITs 3기 임광영**

**학습 날짜 : 2025년 03월 12일 수요일**

---

## 포인터

### 포인터 연산
포인터 연산이란 포인터 변수 자체에 적용할 수 있는 연산이다.

**배정 연산**
포인터 변수에 주소값을 배정하는 연산이다.

**간접 참조 연산**
포인터 변수에 간접 참조 연산자 `*`를 적용하는 연산으로, 포인터 변수가 가리키는 값을 반환한다.

**주소 연산**
포인터 변수가 저장된 메모리의 주소를 `&`을 통해 반환한다.

**증감 및 정수 산술 연산**
포인터 변수가 가지고 있는 주소에 증감(`++`, `--`) 및 산술 연산을 수행한다. 산술 연산은 정수의 덧셈 및 뺄셈 연산만 가능하다.

배열을 가리키는 포인터 변수는 증감 연산을 수행할 때 배열의 범위를 벗어나지 않도록 주의해서 사용해야 한다.
```c
int id[5] = { 100, 200, 300, 400, 500 }, * pid, t = 1, * tp;
pid = id;
tp = &t;

printf("%d\n", ++*pid);  // 101
printf("%d\n", *++pid);  // 200
printf("%d\n", ++*tp);  // 2
printf("%d\n", *++tp);  // 4바이트 뒤에 있는 예상치 못한 값
```

### 포인터와 문자열
문자열을 사용하기 위해서는 문자 배열을 사용하는 방법과 포인터를 사용하는 방법이 있다.
```c
char city1[] = "seoul";  // 문자열 배열로 문자열 선언
char* city2 = "seoul";  // 문자열 포인터로 문자열 선언

// city1 = "busan";  // 새로운 문자열로 문자열 배열 변경 불가능
strcpy(city1, "busan");  // strcpy() 함수 사용
city2 = "busan";  // 문자열 포인터 값 변경 가능

char city3[] = "seoul";
char city4[] = "jeju";
char* pcity3 = city3;

pcity3[0] = 'k';
printf("%s\n", pcity3);  // keoul
// printf("%s\n", *pcity3);  // %s는 데이터 값이 아닌 주소 값을 사용

strcpy(pcity3, "busan");  // strcpy() 함수는 포인터 변수에도 사용 가능
printf("%s\n", pcity3);  // busan

pcity3 = city4;
printf("%s\n", pcity3);  // jeju
```

### 포인터 배열
포인터 자체를 배열로 만들어 사용한다.
```c
char city[3][8] = { "seoul", "jeju", "busan" };
char* pcity[] = {"seoul", "jeju", "busan"};
```

포인터를 이용하여 `jeju`를 접근할 경우 `*(pcity + 1)`을 사용한다. `jeju`의 세 번째 문자인 `j`에 접근할 경우 `*(*(pcity + 1) + 2)`을 사용한다.

### 함수 포인터
함수의 시작 주소를 변수에 저장하여 사용한다. 같은 형태의 함수가 여러 개 있을 때 하나의 함수 포인터로 여러 개의 함수를 호출하여 사용할 경우 유용하다.
```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int max(int, int);
int min(int, int);

int main() {
	int (*fpmm) (int, int);
	int num1, num2, flag;

	printf("두 개의 숫자를 입력하시오.: ");
	scanf("%d %d", &num1, &num2);

	printf("원하는 값을 입력하시오. (1: 큰 값, 2: 작은 값): ");
	scanf("%d", &flag);

	if (flag == 1) {
		fpmm = max;
		printf("두 수 중 큰 값은 %d\n", fpmm(num1, num2));
	} else if (flag == 2) {
		fpmm = min;
		printf("두 수 중 작은 값은 %d\n", fpmm(num1, num2));
	}
}

int max(int x, int y) {
	return x > y ? x : y;
}

int min(int x, int y) {
	return x < y ? x : y;
}
```
이처럼 함수 포인터 변수 `fpmm`을 활용해 쉽게 최댓값 또는 최솟값을 구할 수 있다.