# TIL 250219

**다우기술 FITs 3기 임광영**

**학습 날짜 : 2025년 02월 19일 수요일**

---

## jQuery

### JavaScript
Web 언어는 HTML과 CSS, JavaScript로 구성되며, JavaScript는 동적 쿼리를 처리한다.

### jQuery
jQuery는 모든 브라우저에서 동작하는 Client JavaScript 라이브러리이다. JavaScript는 다음의 특징을 갖고 있기 때문에 코드 작성하기 어렵다는 단점이 있다.
- 함수형 언어
- 객체지향 언어
- Prototype

이에 좀 더 쉽고 강력한 기능을 위해 jQuery가 등장하였다. jQuery의 특징으로는 다음과 같다.
- 쉬운 DOM과 관련된 화면 제어
- 쉬운 이벤트 연결 구현
- 쉬운 시각적인 처리 구현
- **직관적인 Ajax 처리**

jQuery를 이용하기 위해서 CDN(Content Delivery Network) 방식을 사용한다.
```html
<script src="https://code.jquery.com/jquery-3.7.1.min.js" integrity="sha256-/JqT3SQfawRcv/BIHPThkBvs0OEvtFFmqPF/lYI/Cxo=" crossorigin="anonymous"></script>
```

```html
<!--sample.html-->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
    <!--jQuery CDN-->
    <script src="https://code.jquery.com/jquery-3.7.1.min.js" integrity="sha256-/JqT3SQfawRcv/BIHPThkBvs0OEvtFFmqPF/lYI/Cxo=" crossorigin="anonymous"></script>
    <!--User Define JavaScript-->
    <!--CDN 하단에 위치한다.-->
    <script src="js/sample.js"></script>
</head>
<body>
    <!--Element : Block Element와 Inline Element-->
    <h1>jQuery 연습</h1>
    <ul>
        <li>서울</li>
        <li>대구</li>
        <li>부산</li>
    </ul>
    <div>DIV 영역</div>
    <span>SPAN 영역</span>
    <!--가장 쉬운 이벤트 처리 방법은 속성을 이용하는 것이다.-->
    <input type="button" value="클릭" onclick="myFunc()">
</body>
</html>
```
```js
// sample.js

myFunc = () => {
    alert("클릭했습니다.");
}
```

> WebStorm에서 `Alt` + `F2`를 통해 HTML 미리보기를 할 수 있다.

jQuery는 `$(jQuery Object).method()`의 형태로 사용한다. 여기서 `jQuery Object`는 문자열로 된 선택자(Selector) 또는 문서 객체(Document Object)이다.

### jQuery 선택자
**전체 선택자**
전체 선택자 `*`는 가장 기본적인 선택자로, 하위에 있는 모든 문서 객체를 선택하는 선택자이다.
```js
// sample.js

myFunc = () => {
    $("*").css("color", "red");
}
```

**타입 선택자**
타입 선택자는 특정한 태그만 선택하는 선택자이다. 하나 이상의 태그 선택자를 동시에 사용하고 싶을 때는 콤마(`,`)로 구분한다.
```js
// sample.js

myFunc = () => {
    $("h1, div").css("color", "red");
}
```

**아이디 선택자**
HTML 페이지 내에 존재하는 `id` 값은 Unique하다. `id` 속성값을 이용하여 문서 객체를 선택하는 선택자로, 속도 면에서 가장 빠르고 편하다. `#`을 이용한다.
```html
<!--sample.html-->

<body>
    <h1>jQuery 연습</h1>
    <ul>
        <li id="seoul">서울</li>
        <li id="daegu">대구</li>
        <li id="busan">부산</li>
    </ul>
    <div>DIV 영역</div>
    <span>SPAN 영역</span>
    <input type="button" value="클릭" onclick="myFunc()">
</body>
```
```js
// sample.js

myFunc = () => {
    $("#busan").remove();
}
```

**클래스 선택자**
`class` 속성값을 이용하여 문서 객체를 선택하는 선택자로, `.`을 이용한다.
```html
<!--sample.html-->

<body>
    <h1>jQuery 연습</h1>
    <ul>
        <li id="seoul">서울</li>
        <li id="daegu">대구</li>
        <li id="busan">부산</li>
    </ul>
    <div class="divTag">DIV 영역</div>
    <span class="spanTag">SPAN 영역</span>
    <input type="button" value="클릭" onclick="myFunc()">
</body>
```
```js
// sample.js

myFunc = () => {
    $(".divTag").css("background", "blue");
}
```

**자식 선택자와 후손 선택자**
자식 선택자와 후손 선택자는 기본 선택자 뒤에 붙여 범위를 제한한다.
자식 선택자는 `>` 기호를 사용하고 후손 선택자는 공백으로 표시한다.
```html
<!--sample.html-->

<body>
    <h1>jQuery 연습</h1>
    <div>
        <ol>
            <li>이순신</li>
            <li>강감찬</li>
            <li>신사임당</li>
          	<li>홍길동</li>
        </ol>
    </div>
    <ul>
        <li id="seoul">서울</li>
        <li id="daegu">대구</li>
        <li id="busan">부산</li>
    </ul>
    <div class="divTag">DIV 영역</div>
    <span class="spanTag">SPAN 영역</span>
    <input type="button" value="클릭" onclick="myFunc()">
</body>
```
```js
// sample.js

myFunc = () => {
    // $('ol > li').css("color", "yellow");
    $('div li').remove();
}
```

**동위 선택자**
- `element A + element B` : `element A` 바로 다음에 위치하는 `element B` 한 개 선택
- `element A ~ element B` : `element A` 다음에 위치하는 모든 `element B` 여러 개 선택

```html
<!--sample.html-->

<body>
	    <h1>jQuery 연습</h1>
    <div>
        <ol>
            <li>이순신</li>
            <li id="kang">강감찬</li>
            <li>신사임당</li>
            <li>홍길동</li>
        </ol>
    </div>
    <ul>
        <li id="seoul">서울</li>
        <li id="daegu">대구</li>
        <li id="busan">부산</li>
    </ul>
    <div class="divTag">DIV 영역</div>
    <span class="spanTag">SPAN 영역</span>
    <input type="button" value="클릭" onclick="myFunc()">
</body>
```
```js
// sample.js

myFunc = () => {
    alert($("#kang + li").text());
}
```
```js
// sample.js

myFunc = () => {
    alert($("#kang ~ li").text());
}
```

**속성 선택자**
- `element[attribute]` : 특정 속성을 가지고 있는 문서 객체 선택
- `element[attribute=value]` : 속성값이 value와 같은 문서 객체 선택
- `element[attribute~=value]` : 속성값이 value를 단어로써 포함하는 문서 객체 선택
- `element[attribute^=value]` : 속성값이 value로 시작하는 문서 객체 선택
- `element[attribute$=value]` : 속성값이 value로 끝나는 문서 객체 선택
- `element[attribute*=value]` : 속성값이 value를 포함하는 문서 객체 선택

**상태 선택자**
- `:checked` : 체크된 입력 양식을 선택
  ```html
  <!--sample.html-->
  
  <body>
      <input type="checkbox"><span>서울</span><br>
      <input type="checkbox"><span>대전</span><br>
      <input type="checkbox"><span>대구</span><br>
      <input type="checkbox"><span>부산</span><br>
      <input id="region" type="text">
      <input type="button" value="선택된 지역 출력" onclick="myFunc()">
  </body>
  ```
  ```js
  // sample.js

  myFunc = () => {
      // 속성을 이용하여 체크박스를 찾는다.
      // 속성 선택자는 대괄호([])를 이용한다.
    
      // 입력 상자이기 때문에 text() 대신 val()을 사용한다.
      // 인자가 없다면 해당 입력 상자의 값을 가져온다.
      // 인자가 있다면 해당 인자를 입력 상자 안에 넣는다.
      $("#region").val($("[type=checkbox]:checked + span").text());
  }
  ```
- `:disabled` : 비활성화 된 입력 양식을 선택
- `:enabled` : 활성화 된 입력 양식을 선택
- `:focus` : 현재 포커스가 잡혀있는 입력 양식 선택
- `:selected` : `option` 객체 중 선택된 객체를 선택
  ```html
  <!--sample.html-->
  
  <body>
      <select onChange="myFunc()">
          <option>서울</option>
          <option>대구</option>
          <option>부산</option>
          <option>광주</option>
      </select>
      <input type="text">
  </body>
  ```
  ```js
  // sample.js
  
  myFunc = () => {
      $("input").val($("select > option:selected").text());
  }
  ```

### jQuery Method
**배열 처리**
`each()` 메서드를 활용한다.
```html
<!--sample.html-->

<body>
    <input type="checkbox"><span>서울</span><br>
    <input type="checkbox"><span>대전</span><br>
    <input type="checkbox"><span>대구</span><br>
    <input type="checkbox"><span>부산</span><br>
    <input id="region" type="text">
    <input type="button" value="선택된 지역 출력" onclick="myFunc()">
</body>
```
```js
// sample.js

myFunc = () => {
    $("[type=checkbox]:checked + span").each((index, item) => {
        // item : 선택된 문서 객체(Document Object)로 jQuery 객체가 아니다.
        // 따라서 jQuery 객체로 바꿔서 사용해야 한다.
        console.log($(item).text() + "가 선택되었습니다.");
    });
}
```

**`remove()`**
해당 선택자를 삭제한다.
```js
// sample.js

myFunc = () => {
    $("h1, div").remove();
}
```

**`text()`**
해당 선택자의 텍스트를 반환한다.
```js
// sample.js

myFunc = () => {
    alert($("#kang + li").text());
}
```

**`$()`**
새로운 element를 생성 후 원하는 위치에 추가한다.
```html
<!--sample.html-->

<body>
    <ul>
        <li>홍길동</li>
        <li>강감찬</li>
        <li>신사임당</li>
    </ul>
    <input type="text" id="name">
    <input type="button" value="사람 추가" onclick="myFunc()">
</body>
```
```java
// sample.java

myFunc = () => {
    let newName = $("#name").val();
    let li = $("<li></li>").text(newName);

    // 원하는 위치에 태그를 추가하는 방법

    // 1. append() : 제일 마지막 자식에 붙여넣는다.
    $("ul").append(li);

    // 2. prepend() : 제일 처음 자식에 붙여넣는다.
    $("ul").prepend(li);

    // 3. after() : 바로 다음 형제에 붙여넣는다.
    $("ul > li:first").after(li);

    // 4. before() : 바로 이전 형제에 붙여넣는다.
    $("ul > li:last").before(li);
}
```

**`attr()`**
특정 element의 속성을 추가한다.
```js
// sample.js

myFunc = () => {
    let imgTag = $("<img />").attr("src", "https://img.freepik.com/free-photo/pet-dog-breed-animal-portrait-puppy-mammal_53876-30297.jpg").attr("width", "200px");

    $("#name").after(imgTag);
}
```

### jQuery Event
`on()` 메서드를 활용한다.
```html
<!--sample.html-->

<body>
    <h1>Hello World</h1>
    <h1>Hello jQuery</h1>

    <input type="button" value="이벤트 처리" onclick="myFunc()">
</body>
```
```js
// sample.js

myFunc = () => {
    $("h1").on("click", function () {
        // this : 현재 사용되는 객체에 대한 Reference
        // jQuery의 Event 처리 코드 내에서의 this는 Event Source를 지칭한다.
        alert($(this).text());
    })
}
```

>
```js
myFunc = () => {
  $("h1").on("click", () => {
    alert($(this).text());
  })
}
```
해당 코드에서 화살표 함수를 사용하면 작동하지 않는다. 화살표 함수의 `this`는 `window` 객체를 가리키기 때문에 `function` 키워드를 사용한다.

>
|일반 함수|화살표 함수|
|---|---|
|실행할 때마다 `this`가 달라짐|`this`가 상위 스코프(부모)의 `this`를 가리킴|
|이벤트 핸들러에서 `this` 이벤트가 발생한 요소<br>(`event.target`)|함수를 선언한 위치의 `this`<br>(주로 `window`)|

## jQuery AJAX

AJAX에서 데이터는 JSON(JavaScript Object Notation) 형식으로 전송한다. JSON은 일반 문자열로써 특정 프로그래밍 언어와 연결되어 있지 않기 때문에, 범용적으로 사용할 수 있다.

JSON을 사용하기 위해서는 객체로 변환 후 사용한다.

### 영화정보 프로젝트 with AJAX
```html
<!--boxOffice.html-->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Box Office</title>
    <script src="https://code.jquery.com/jquery-3.7.1.min.js" integrity="sha256-/JqT3SQfawRcv/BIHPThkBvs0OEvtFFmqPF/lYI/Cxo=" crossorigin="anonymous"></script>
    <script src="js/boxOffice.js"></script>
</head>
<body>
    <h1>1등 영화 제목</h1>

    <input type="button" value="AJAX 호출" onclick="myFunc()">
</body>
</html>
```
```js
// boxOffice.js

myFunc = () => {
    // jQuery로 AJAX 호출
    // API 파라미터 객체를 JavaScript로 생성한다.
    $.ajax({
        async: true,  // 비동기 방식(기본값)
        url: "http://www.kobis.or.kr/kobisopenapi/webservice/rest/boxoffice/searchDailyBoxOfficeList.json",
        type: "GET",
        data: {
            key: "77cf700778ce164936dd6939079db46e",
            targetDt: "20250218"
        },
        dataType: "json",  // 기본값
        success: (data) => {
            // JSON 문자열을 객체로 변환시킨 객체가 data 변수에 매핑된다.
            alert("호출이 성공했습니다.");
            $("h1").text(data.boxOfficeResult.dailyBoxOfficeList[0].movieNm);
        },
        error: () => {
            alert("호출이 실패했습니다.");
        }
    })
}
```