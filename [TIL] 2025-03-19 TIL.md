# TIL 250319

**다우기술 FITs 3기 임광영**

**학습 날짜 : 2025년 03월 19일 수요일**

---

## 최적화

### 메모리 최적화
최소한의 메모리로 데이터를 저장하고 공간을 절약하는 것이다. 비트 필드나 비트 연산자를 활용한다.

### 속도 최적화
연산 속도를 높여 프로그램이 더 빠르게 실행되도록 하는 것이다. 구조체 정렬을 CPU 워드 크기에 맞추거나 비트 연산을 활용한다.

**CPU워드 크기**
CPU는 4 바이트 또는 8 바이트 정렬을 선호한다. 따라서 데이터를 4 바이트 단위로 묶으면 더 빠르게 읽고 쓸 수 있다.
```c
typedef struct {
	uint32_t flags;
} AccountStatus;
```

**비트 연산**
비트 연산자 `&`, `|`, `~`을 이용한다.

### 가독성 최적화
코드를 이해하기 쉽도록 개선하는 것을 말하며, `enum`이나 `#define`을 활용하여 작성한다.

## 비트

### 비트 필드
비트 필드는 구조체 내에서 변수를 비트 단위로 저장할 수 있도록 해주는 기능이다. 즉, 여러 개의 `Boolean` 값이나 작은 숫자를 한 개의 정수형 변수(`unsigned int`)에 단위로 저장하는 방법이다.

예시로 고객의 계좌 상태를 다음과 같이 관리할 수 있다.
- 활성화 여부 (1 비트)
  - `1`이면 활성, `0`이면 비활성.
- VIP 여부 (1 비트)
  - `1`이면 VIP, `0`이면 일반.
- 대출 여부 (1 비트)
  - `1`이면 대출 있음, `0`이면 없음.
- 예금 보호 한도 초과 여부 (1 비트)
  - `1`이면 초과, `0`이면 초과하지 않음.
- 잔액 상태 (3 비트)
  - `000`: 0원.
  - `001`: 1 ~ 10만원.
  - `010`: 10 ~ 50만원.
  - `011`: 50 ~ 100만원.
  - `100`: 100 ~ 500만원.
  - `101`: 500 ~ 1000만원.
  - `110`: 1000 ~ 5000만원.
  - `111`: 5000만원 이상.

이러한 비트 필드를 구조체로 정의하면 다음과 같다.
```c
#include <stdio.h>

// 비트 필드를 사용한 구조체 정의
typedef struct {
    unsigned int is_active : 1;        // 활성화 여부 (1 비트)
    unsigned int is_vip : 1;           // VIP 여부 (1 비트)
    unsigned int has_loan : 1;         // 대출 여부 (1 비트)
    unsigned int over_protection : 1;  // 예금 보호 한도 초과 여부 (1 비트)
    unsigned int balance_status : 3;   // 잔액 상태 (3 비트)
} AccountStatus;

// 계좌 상태 출력 함수
void printAccountStatus(AccountStatus account) {
    printf("계좌 상태:\n");
    printf("- 활성화: %s\n", account.is_active ? "예" : "아니오");
    printf("- VIP: %s\n", account.is_vip ? "예" : "아니오");
    printf("- 대출 여부: %s\n", account.has_loan ? "있음" : "없음");
    printf("- 예금 보호 한도 초과: %s\n", account.over_protection ? "초과" : "초과하지 않음");
    
    const char* balance_labels[] = {
        "0원", "1 ~ 10만 원", "10 ~ 50만 원", "50 ~ 100만 원",
        "100 ~ 500만 원", "500 ~ 1000만 원", "1000 ~ 5000만 원", "5000만 원 이상"
    };
    printf("- 잔액 상태: %s\n", balance_labels[account.balance_status]);
}

int main() {
    // 계좌 상태 설정 (예제: 활성화, VIP, 대출 있음, 보호 초과, 잔액 500 ~ 1000만 원)
    AccountStatus customer1 = { 1, 1, 1, 1, 5 };
    
    printAccountStatus(customer1);
    
    printf("AccountStatus 구조체 크기: %lu 바이트\n", sizeof(AccountStatus));  // 4 바이트

    return 0;
}
```
비트 필드를 올바르게 사용하기 위해 `unsigned int`를 사용한다.


> 총 7 비트를 부여하였지만 실제로 구조체는 4 바이트를 차지한다. 만약 구조체 정의를 `unsigned int`가 아닌 `uint8_t`를 사용한다면 해당 구조체는 1 바이트를 차지한다.
>
|자료형|크기 (바이트)|비트 개수|
|---|---|---|
|`unsigned int`|4 바이트|32 비트|
|`uint8_t`|1 바이트|8 비트|

### 비트 연산
직접 비트 연산을 하면 메모리 낭비를 줄이고 성능을 향상시킬 수 있다. 비트 연산을 위한 자료형을 위해 `stdint.h` 헤더 파일을 사용한다.

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdint.h>

// 비트 연산을 사용한 계좌 상태 구조체
typedef struct {
    uint8_t flags;  // 1 바이트(8 비트)로 상태 저장
} AccountStatus;

// 비트 마스크 정의
#define ACTIVE_MASK 0x01        // 0000 0001 (1 비트)
#define VIP_MASK 0x02           // 0000 0010 (2 비트)
#define LOAN_MASK 0x04          // 0000 0100 (3 비트)
#define OVER_PROTECT_MASK 0x08  // 0000 1000 (4 비트)
#define BALANCE_MASK 0x70       // 0111 0000 (5 ~ 7 비트)

// 잔액 상태 매크로
#define BALANCE_SHIFT 4

// 계좌 상태 설정 함수
void setAccountStatus(AccountStatus* account, int is_active, int is_vip, int has_loan, int over_protection, int balance_status) {
    account->flags = (is_active ? ACTIVE_MASK : 0) |
        (is_vip ? VIP_MASK : 0) |
        (has_loan ? LOAN_MASK : 0) |
        (over_protection ? OVER_PROTECT_MASK : 0) |
        ((balance_status << BALANCE_SHIFT) & BALANCE_MASK);
}

// 계좌 상태 출력 함수
void printAccountStatus(AccountStatus account) {
    printf("계좌 상태:\n");
    printf("- 활성화: %s\n", (account.flags & ACTIVE_MASK) ? "예" : "아니오");
    printf("- VIP: %s\n", (account.flags & VIP_MASK) ? "예" : "아니오");
    printf("- 대출 여부: %s\n", (account.flags & LOAN_MASK) ? "있음" : "없음");
    printf("- 예금 보호 한도 초과: %s\n", (account.flags & OVER_PROTECT_MASK) ? "초과" : "초과하지 않음");
    
    const char* balance_labels[] = {
        "0원", "1 ~ 10만 원", "10 ~ 50만 원", "50 ~ 100만 원",
        "100 ~ 500만 원", "500 ~ 1000만 원", "1000 ~ 5000만 원", "5000만 원 이상"
    };
    int balance_status = (account.flags & BALANCE_MASK) >> BALANCE_SHIFT;
    printf("- 잔액 상태: %s\n", balance_labels[balance_status]);
}

int main() {
    AccountStatus customer1;

    // 계좌 상태 설정 (활성화, VIP, 대출 있음, 보호 초과, 잔액 500 ~ 1000만 원)
    setAccountStatus(&customer1, 1, 1, 1, 1, 5);

    printAccountStatus(customer1);

    printf("\nAccountStatus 구조체 크기: %lu 바이트\n", sizeof(AccountStatus));
    return 0;
}
```

> 비트 마스크 값은 2의 제곱수를 활용해 독립적으로 설정하도록 한다.

> **`BALANCE_MASK`**
`balance_status << BALANCE_SHIFT`을 통해 시프트 연산을 수행하기만 해도 `balance_status` 값은 올바르게 전달된다. 그럼에도 `BALANCE_MASK`와 `&` 연산을 수행하는 이유는 범위를 벗어날 수 있기 때문이다.
만약 `balance_status` 값이 `7` 값을 넘어가게 되면 해당 값은 다른 비트에 영향을 줄 수 있다. 따라서 `BALANCE_MASK`와의 비트 마스킹을 통해 범위를 제한해야한다.
결국 비트 마스크는 사용하는 비트의 범위를 제한하는 중요한 역할을 한다.