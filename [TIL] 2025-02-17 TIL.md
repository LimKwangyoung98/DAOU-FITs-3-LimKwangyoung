# TIL 250217

**다우기술 FITs 3기 임광영**

**학습 날짜 : 2025년 02월 17일 월요일**

---

## Web Programming

웹 프로그래밍은 Client-Server 구조로 이루어져 있다.

### Client Side
Web Client 또는 Web Browser가 있으며 대표적으로 Chrome을 사용한다.

### Server Side
대표적으로 Tomcat을 사용하며 Web Server과 WAS(Web Application Server)로 구성된다. Web Server는 정적 리소스를 담당하며 WAS는 동적 리소스를 담당한다.

### 웹 구동의 기본
![](https://velog.velcdn.com/images/pyoung/post/e0c0dc79-b6fe-4151-ad0b-c46be5003efb/image.png)


![](https://velog.velcdn.com/images/pyoung/post/42d0f822-64a8-400a-bfa3-e8568ead2a41/image.png)


- Web Client가 Server에게 HTTP 프로토콜에 따른 요청(Request) 문자열을 보냄
- Web Server가 처리할 수 있으면 Web Client에게 HTTP 프로토콜에 따른 응답(Response) 문자열을 보냄

> Web Server의 포트번호는 `8080`이다.

## Servlet 기본

### 웹 프로젝트 생성
프로젝트 생성 단계는 다음과 같다.
![](https://velog.velcdn.com/images/pyoung/post/bac6aec4-a0a4-4be7-8442-c0cd59a80d65/image.png)

![](https://velog.velcdn.com/images/pyoung/post/2ecc9db7-a256-4bb5-881c-0ae8bfa810b9/image.png)

> 프로젝트 생성 시 JDK와 Tomcat, Jakarta EE 버전을 맞춘다.

### 정적 리소스
Client에게 보여줄 정적 리소스인 HTML 파일을 `webapp` 파일에 작성한다.
```html
<!--webapp.hello.html-->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
</head>
<body>
    Hello Servlet!
    <br>
    정적 Resource를 Access합니다.
</body>
</html>
```

프로젝트의 `[실행/디버그 구성]`은 다음과 같다.
![](https://velog.velcdn.com/images/pyoung/post/5bf19eb5-a0db-4b93-ad77-fd20f6fb5576/image.png)

> `[애플리케이션 컨텍스트]`를 수정하면 정적 리소스가 Client에게 전달되는 URL이 변경된다.

### 동적 리소스
Java 프로그램인 Servlet을 작성하고 Client가 이를 호출해서 해당 결과를 렌더링하고자 한다.

Servlet은 인터페이스이며, 이를 구현하는 `GenericServlet` 클래스와 상속하는 `HttpServlet` 클래스가 있다. 따라서 직접 구현하는 것이 아니라 `HttpServlet` 클래스를 상속해서 Servlet을 구현한다.

`HttpServlet` 클래스를 상속함으로써 상속되는 메서드는 다음과 같다.
- `init()` : Servlet 객체가 생성될 때 호출되는 초기화를 담당하는 메서드
  - 생성자 대신 `init()`을 이용해 초기화를 진행.
- `service()` : Client가 Servlet을 실생할 때 호출되는 메서드
  - Client의 요청이 GET이면 `doGet()` 호출.
  - Client의 요청이 POST이면 `doPost()` 호출.
  - 이처럼 이미 정해진 역할이 있기 떄문에 일반적으로 Overriding 하지 않음.
- `doGet()`, `doPost()` : 해당 메서드를 Overriding하여 Client가 원하는 작업을 수행
- `destroy()` : Servlet 인스턴스가 WAS에서 삭제될 때 수행되는 메서드
  - 사용하고 있는 여러 리소스를 해제해야 함.

> 일반적으로 Client가 Server의 프로그램을 호출할 때는 GET 방식과 POST 방식이 있다.

## 동적 리소스

Servlet을 작성할 Java 프로그램을 `java` 파일의 패키지 안에 작성한다.

클라이언트가 GET 방식으로 Servlet을 호출했을 때 응답을 전송하는 Servlet을 구현하기 위해서는 우선 클라이언트가 Servlet을 호출하는 방법을 제공해야 한다. 이는 Servlet에 논리적인 이름(URL Mapping)을 부여하여 이를 통해 호출하도록 한다.

1. `xml` 파일을 이용
	- `xml` 파일을 이용하기 때문에 번거로움.
    - 전체적인 이름 관리를 하나의 파일에서 할 수 있기 때문에 유지보수 측면에서 이점.
2. `WebServlet` Annotaion 이용
	- 일반적이며 직관적인 방식.
	- `value` : 클라이언트가 사용할 이름.

```java
// java.org.example.helloservletproject.MyFirstServlet.java

import javax.servlet.ServletException;
import javax.servlet.ServletRequest;
import javax.servlet.ServletResponse;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import java.io.IOException;

// HttpServlet을 상속함으로써 해당 클래스는 자동으로 Servlet이 된다.
@WebServlet(value = "/myServlet")
public class MyFirstServlet extends HttpServlet {
    public MyFirstServlet() {
        System.out.println("생성자 호출");
    }

    @Override
    public void init() throws ServletException {
        super.init();

        // 초기화 코드
        System.out.println("init() 메소드 호출");
    }

    // 일반적으로 Overriding 하지 않음
    // @Override
    // public void service(ServletRequest req, ServletResponse res) throws ServletException, IOException {
    //     super.service(req, res);
    // }

    @Override
    protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException {
        // 클라이언트 요청이 GET ➡ 입력 처리 ➡ 로직 처리 ➡ 출력 처리
        System.out.println("doGET() 메소드 호출");
    }

    @Override
    protected void doPost(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException {
        // 클라이언트 요청이 POST ➡ 입력 처리 ➡ 로직 처리 ➡ 출력 처리
        System.out.println("doPOST() 메소드 호출");
    }

    @Override
    public void destroy() {
        // 리소스 해제
        System.out.println("destroy() 메소드 호출");

    }
}
```

이는 `http://localhost:8080/HelloServletProject/myServlet`을 통해 해당 Servlet에 접근할 수 있다.

### 출력 처리
`doGet()` 메소드를 활용한 출력 처리는 다음과 같다.
```java
@Override
protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException {
	// 클라이언트 요청이 GET ➡ 입력 처리 ➡ 로직 처리 ➡ 출력 처리

	// Servlet이 Client에게 출력 처리

	// 1. 데이터 통로를 통해 데이터를 전달해야 하며
    //    전달되는 데이터의 형태를 지정해야 한다.
    resp.setContentType("text/html;charset=utf-8");
    // 2. 결과를 전달하기 위한 데이터 통로를 연다.
    PrintWriter out = resp.getWriter();
    // 3. 데이터 통로를 통해 데이터를 전송한다.
    out.println("<html>");
    out.println("<head></head>");
    out.println("<body>");
    out.println("정상적으로 호출되었습니다.");
    out.println("</body>");
    out.println("</html>");
    out.flush();  // 명시적으로 작성해주는 것이 좋다.
    out.close();
}
```
`printWriter`를 이용해 Client에게 데이터 연결 통로를 열고 데이터를 전달한다. 이때 `ContentType` 설정에 주의한다.

### 입력 처리
입력 처리 방식으로는 GET 방식과 POST 방식으로 나뉜다.

**GET 방식**
Query String을 이용해 데이터를 전달하는 방식을 이용한다. Query String은 특수한 형식을 가진 문자열이며 `?변수=값&변수=값`의 형태이다.
이를 Server(Servlet)에서는 전달된 데이터를 `getParameter("변수명")`을 통해 받는다.
```java
@Override
protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException {
	// 클라이언트 요청이 GET ➡ 입력 처리 ➡ 로직 처리 ➡ 출력 처리

	// Servlet이 Clinet으로부터 입력 처리

	// Query String으로부터 전달되는 데이터 타입은 무조건 문자열이다.
    String name = req.getParameter("name");
    String age = req.getParameter("age");
    String address = req.getParameter("address");

	resp.setContentType("text/html;charset=utf-8");
	PrintWriter out = resp.getWriter();
	out.println("<html>");
    out.println("<head></head>");
    out.println("<body>");
    out.println(name + " " + age + " " + address);
    out.println("</body>");
    out.println("</html>");
    out.flush();
    out.close();
}
```

GET 방식은 다음과 같은 문제점이 있다.
- 데이터를 Query String으로 사용하기 때문에 보안에 취약점이 있음
  - 128bit 암호화를 통해 처리해야 함.
- URL의 길이 제한으로 인해 데이터 길이가 길 경우 전달하기 어려움

**POST 방식**
POST 방식은 Query String 방식을 사용하지 않기 때문에 GET 방식보다는 보안성을 갖고 있다. 또한 Request Header에 포함하여 데이터를 전달하기 때문에 길이의 제한이 없다.

POST 방식은 `<form>` 태그를 이용해서 Server 쪽의 프로그램(Servlet)을 호출하는 구조이다. 우선 Client에게 보여줄 HTML이 필요하다.
```html
<!--webapp.input.html-->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
</head>
<body>
    <h1>Server에 전달할 데이터를 입력하세요.</h1>
    <form action="http://localhost:8080/HelloServletProject/myServlet" method="POST">
        이름 : <input name="name" type="text" />
        <br><br>
        나이 : <input name="age" type="number" />
        <br><br>
        <input type="submit" value="서버로 전송">
    </form>
</body>
</html>
```

`doPost()` 메서드 작성 시 `<input>` 태그에서 설정한 `name` 값과 매핑한다.
```java
@Override
protected void doPost(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException {
	// 클라이언트 요청이 POST ➡ 입력 처리 ➡ 로직 처리 ➡ 출력 처리

	// 입력 데이터에 유니코드(한글)가 포함되어 있을 경우
    req.setCharacterEncoding("utf-8");

	String name = req.getParameter("name");
    String age = req.getParameter("age");

	resp.setContentType("text/html;charset=utf-8");
    PrintWriter out = resp.getWriter();
    out.println("<html>");
    out.println("<head></head>");
    out.println("<body>");
    out.println(name + " " + age + " ");
    out.println("</body>");
    out.println("</html>");
    out.flush();
    out.close();
}
```

### 로직 처리
직접 로직 처리를 하지는 않는다. 서비스 객체를 통해 로직 처리를 하며 그 결과를 화면에 출력한다. 즉 MVC 패턴의 관점에서 보면 Servlet은 Controller의 역할을 수행한다.

## Servlet with MyBatis

### `resources` 폴더
```
# driver.properties

db.driver=oracle.jdbc.driver.OracleDriver
db.url=jdbc:oracle:thin:@localhost:1521:xe
db.username=C##DEV
db.password=1234
```
```xml
<!--sqlmap.SqlMapConfig.xml-->

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org/DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>
    <properties resource="./driver.properties" />

    <settings>
        <setting name="jdbcTypeForNull" value="NULL" />
    </settings>

    <typeAliases>
        <typeAlias alias="Book" type="org.example.booksearchservletproject.vo.BookVO" />
    </typeAliases>

    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC" />

            <dataSource type="POOLED">
                <property name="driver" value="${db.driver}" />
                <property name="url" value="${db.url}" />
                <property name="username" value="${db.username}" />
                <property name="password" value="${db.password}" />
            </dataSource>
        </environment>
    </environments>

    <mappers>
        <mapper resource="./sqlmap/Book.xml" />
    </mappers>
</configuration>
```
```xml
<!--Book.xml-->

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org/DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="servlet.booksearch">
    <select id="selectByKeyword" parameterType="Map" resultType="org.example.booksearchservletproject.vo.BookVO">
        <![CDATA[
        SELECT bisbn, btitle, bprice
        FROM book
        WHERE btitle LIKE '%' || #{keyword} || '%'
          AND bprice <= #{price}
        ]]>
    </select>

    <select id="selectByISBN" parameterType="String" resultType="org.example.booksearchservletproject.vo.BookVO">
        <![CDATA[
        SELECT bisbn, btitle, bdate, bpage, bprice, bauthor, btranslator, bpublisher
        FROM book
        WHERE bisbn = #{bisbn}
        ]]>
    </select>
</mapper>
```

### `webapp` 폴더
View 역할에 해당하는 HTML 파일은 다음과 같다.
```html
<!--webapp.bookSearch.html-->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Book Search</title>
</head>
<body>
<h1>도서검색 프로그램</h1>
<form action="bookSearch" method="POST">
    <div>
        도서명 키워드 : <input type="text" name="keyword" />
    </div>
    <br>
    <div>
        <input type="radio" name="price" value="10000">10,000원 미만<br>
        <input type="radio" name="price" value="20000">20,000원 미만<br>
        <input type="radio" name="price" value="30000">30,000원 미만<br>
        <input type="radio" name="price" value="40000">40,000원 미만
        <br><br>
        <input type="submit" value="도서 검색">
    </div>
</form>
</body>
</html>
```

### `controller` 패키지
Controller의 역할을 하는 Servlet 파일은 다음과 같다.

### `service` 패키지