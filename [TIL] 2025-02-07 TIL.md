# TIL 250207

**다우기술 FITs 3기 임광영**

**학습 날짜 : 2025년 02월 07일 금요일**

---

## String Class

### `String`

Java는 `char`를 Primitive Type으로 제공하고 있지만, 문자열 `String`의 경우 클래스이다. 일반적으로 클래스는 `new` 키워드를 통해 인스턴스를 생성하지만 `String` 클래스는 단순 할당으로도 가능하다.

인스턴스가 생성되는 Heap 영역에는 String Pool이라는 영역이 있다. `""`을 통해 문자열 인스턴스를 생성하면 String Pool에 들어가며, `new` 키워드를 통해 생성하면 일반적인 Heap 영역에 들어간다.
`String` 인스턴스는 immutable의 성격을 갖고 있다.

```java
public class StringTest {
    public static void main(String[] args) {
        String str1 = "Hello";
        String str2 = "Hello";
        String str3 = new String("Hello");

        System.out.println(str1 == str2);       // true
        System.out.println(str1 == str3);       // false
        System.out.println(str1.equals(str3));  // true
    }
}
```

![](https://velog.velcdn.com/images/pyoung/post/552f7eab-032c-4e3d-9e88-335d01296bd2/image.png)

> `==` 연산자는 Primitive Type의 경우 값을 비교하고, Reference Type의 경우 주소값을 비교한다. 따라서 문자열의 경우 `==`가 아닌 `equals`를 사용한다.

## Array Class

배열은 같은 Type의 데이터를 여러 개 저장할 수 있는 자료구조이다. 배열을 사용하기 위해서는 `[]`을 이용한다.

배열은 인스턴스이며 이를 위해서 `new` 키워드를 사용한다.

```java
public class ArrayTest {
    public static void main(String[] args) {
        int[] arr1 = new int[3];
        String[] arr2 = new String[3];
    }
}
```

### ArrayList
ArrayList는 다음과 같은 특징을 지니고 있다.
- 배열과 유사
- 크기가 유동적
- 다른 Data Type을 저장할 수 있음
  - 일반적으로 하나의 Data Type을 저장.
- **인스턴스(Reference Type)만 저장할 수 있음**
  - Java는 Wrapper Class를 제공하기 때문에 Primitive Type도 저장 가능.
  
```java
import java.util.List;
import java.util.ArrayList;

public class ArrayListTest {
    public static void main(String[] args) {
        // ArrayList
        List list = new ArrayList();
        
        list.add("홍길동");  // String 인스턴스 저장
        list.add("안녕안녕");

        // ArrayList는 Primitive Type을 저장할 수 없다.
        // Java는 기본적으로 Primitive Type에 대해 Wrapper Class를 지원한다.
        // 따라서 Autoboxing이 발생하여 Primitive Type이 Reference Type으로 만들어지며
        // ArrayList에 저장할 수 있다.
        list.add(100);
        // list.add(new Integer(100));
        
        list.add(true);

        for (Object ele : list) {
            // ele 인스턴스의 toString() 메서드 호출
            System.out.println(ele);
        }
    }
}
```

일반적으로 ArrayList에는 하나의 Data Type만 저장하기 때문에 Generic을 사용하여 선언한다.
```java
import java.util.ArrayList;
import java.util.List;

public class ArrayListTest {
    public static void main(String[] args) {
        // Generic ArrayList
        List<String> list = new ArrayList<String>();

        list.add("홍길동");
        list.add("안녕안녕");
        
		System.out.println(list.get(0));  // 홍길동
    }
}
```

> `ArrayList`의 상위 클래스 중 하나로 `List` 인터페이스가 있다. `ArrayList`를 선언할 때 일반적으로 `List` 타입을 사용하며, 이는 프로그램의 유연성을 높인다.

### HashMap
HashMap은 (key, value) 쌍으로 이루어진 데이터를 저장할 수 있는 자료구조이다. 특징은 다음과 같다.
- 집합형 자료구조
- key, value의 쌍으로 저장
- key와 value 모두 인스턴스만 사용
- 순서 개념이 없음
  - 순서 개념이 있는 HashMap도 존재
  
```java
import java.util.Map;
import java.util.HashMap;

public class HashMapTest {
    public static void main(String[] args) {
        Map<Integer, String> map = new HashMap<Integer, String>();

        map.put(1, "홍길동");
        map.put(2, "신사임당");

        System.out.println(map.get(2));  // 신사임당

        for (Integer key : map.keySet()) {
            System.out.println(key + " - " + map.get(key));
        }
    }
}
```
  
> `HashMap`의 상위 클래스 중 하나로 `Map` 인터페이스가 있다. `HashMap`를 선언할 때 일반적으로 `Map` 타입을 사용한다.

### HashSet
HashSet의 대표적인 특징으로는 중복을 제거해준다는 것이다. 또한 HashSet은 순서가 없기 때문에 이를 순회할 때는 `Iterator`을 활용할 수 있다.
```java
import java.util.Set;
import java.util.HashSet;
import java.util.Iterator;

public class HashSetTest {
    public static void main(String[] args) {
        Set<String> set = new HashSet<String>();

        set.add("홍길동");
        set.add("신사임당");
        set.add("강감찬");
        set.add("홍길동");  // 추가되지 않는다.
		
        // for 문
        for (String ele : set) {
            System.out.println(ele);
        }
		
        // while 문
        Iterator<String> iter = set.iterator();
        while (iter.hasNext()) {
            System.out.println(iter.next());
        }
    }
}
```

## Exception Class

### Exception
특정 상황이 발생하면 Exception Class의 인스턴스를 JVM이 생성한다. 해당 인스턴스가 처리되지 않으면 프로그램이 비정상 종료된다. 이를 처리하는 유일한 방법은 `try`와 `catch` 구문이다.

```java
public class ExceptionTest {
    public static void main(String[] args) {
        // ArithmeticException
        // int result = 10 / 0;

        // NullPointerException
        // Object obj = null;
        // System.out.println(obj.toString());

        Object obj = null;
        try {
            int result = 10 / 0;
            System.out.println(obj.toString());
        } catch (ArithmeticException e1) {
            // 예외상황 처리 코드
            // 프로그램이 비정상 종료되지 않는다.
            System.out.println("ArithmeticException 발생");
        } catch (NullPointerException e2) {
            // 예외상황 처리 코드
            // 프로그램이 비정상 종료되지 않는다.
            System.out.println("NullPointerException 발생");
        } catch (Exception e3) {
        	// 예외상황 처리 코드
            // 프로그램이 비정상 종료되지 않는다.
        	System.out.println("Exception 발생");
        } finally {
            System.out.println("무조건 실행");
        }
        System.out.println("프로그램 정상 종료");
    }
}
```