# TIL 250318

**다우기술 FITs 3기 임광영**

**학습 날짜 : 2025년 03월 18일 화요일**

---

## 메모리 구조

메모리 구조는 Text Segment, Data Segment, BSS Segment, Heap, Stack으로 구성되어 있다.
![](https://velog.velcdn.com/images/pyoung/post/ffdb1aa2-7c11-4538-9020-b7ee692bb49f/image.png)

### Text Segment
프로그램의 실행 코드가 저장된다. 읽기 전용으로 코드가 실행되는 동안 수정정이 불가능하다. 프로그램의 모든 함수와 명령어가 포함되며 이는 컴파일 시에 결정된다. 메모리 보호를 위해 일반적으로 읽기 전용 속성을 가진다.

### Data Segment
초기화 된 전역 변수 및 정적 변수를 저장한다. 즉, 초기값이 있는 전역 및 정적 변수가 저장되며, 이는 프로그램 실행 전 메모리에 로드된다.

### BSS Segment
초기화 되지 않은 전역 변수 및 정적 변수를 저장한다. 실행 시 `0`으로 자동 초기화된다.

### Heap
동적으로 할당된 메모리가 저장되는 영역으로, `malloc`, `calloc`, `realloc`, `free`와 같은 함수를 통해 관리된다.

> Heap 영역의 크기는 제한 없음(동적 관리) $\rightarrow$ 메모리 누수 발생
$\rightarrow$ 프로그램 실행 중 필요에 따라 할당 및 해제

### Stack
함수 호출 시 지역 변수와 매개 변수, 반환 주소 등이 저장되는 영역이다. LIFO 방식으로 작동하기 때문에 가장 최근에 호출된 함수가 먼저 종료된다. 함수가 호출되면 Stack Frame이 생성되고, 함수가 종료되면 해당 Stack Frame이 해제된다.

> Stack 영역의 크기는 제한적
$\rightarrow$ 너무 많은 메모리 사용 $\rightarrow$ Stack 오버 플로우 발생

> **C 언어와 Java의 메모리 구조**
>
|구분|C 언어|Java|
|---|---|---|
|메모리 관리|프로그래머가 직접 관리|Garbage Collection을 통해 자동 관리|
|객체 저장|객체의 동적 메모리 할당을 위해 힙 사용|모든 객체가 힙에 저장|

## 동적 할당

### `malloc()`
동적 메모리를 할당하는 함수로, 런타임동안 힙 영역에 메모리를 할당한다. 초기화되지 않은 메모리 블록의 포인터를 반환하며, 할당된 메모리는 반드시 `free()` 함수를 통해 해제해야 한다.

```c
void* malloc(size_t size);
```
여기서 `size_t size`는 바이트 크기이며, `void*`는 메모리 할당 성공 후 반환된 메모리 블록의 시작 주소이다. 만약 할당되지 않으면 `NULL`을 반환한다.

반환 타입이 `void*`이기 때문에 형변환을 해야한다.

정수 4바이트를 동적 할당하는 코드는 다음과 같다.
```c
#include <stdio.h>
#include <stdlib.h>  // 동적 할당을 위한 헤더 파일

int main() {
    int* ptr = (int*)malloc(sizeof(int));  // int 바이트 크기만큼 동적 할당

    if (ptr == NULL) {
        printf("메모리 할당 실패\n");
        return 0;
    }

    *ptr = 42;  // 할당된 메모리에 값 저장
    printf("동적 메모리의 값: %d\n", *ptr);
    free(ptr);  // 메모리 해제

	return 0;
}
```

배열의 크기를 할당하는 코드는 다음과 같다.
```c
#include <stdio.h>
#include <stdlib.h>

int main() {
	int N = 5;
	int* arr = (int*)malloc(N * sizeof(int));  // 정수 5개 크기의 배열 할당

	if (arr == NULL) {
		printf("메모리 할당 실패\n");
		return 0;
	}

	// 동적 배열 초기화
	for (int i = 0; i < N; i++) {
		arr[i] = i + 1;
	}

	// 배열 출력
	printf("동적 배열 요소: ");
	for (int i = 0; i < N; i++) {
		printf("%d ", arr[i]);
	}
	printf("\n");
	free(arr);  // 메모리 해제

	return 0;
}
```

> `int arr[N];` 역시 정수 `N`개 크기의 배열을 할당한다. 하지만 이러한 정적 할당은 무조건 `N` 값이 상수여야 한다는 특징이 있다.

`malloc()`을 사용할 때 초기화 되지 않은 메모리 블록은 쓰레기 값이 들어가기 때문에, 별도의 초기화 설정이 필요하다.

### `calloc()`
동적 메모리를 할당하는 함수로, `0`으로 초기화 된 메모리 블록을 반환한다. 이는 연속된 여러 개의 요소를 한 번에 할당할 때 유용하다.

```c
void* calloc(size_t num, size_t size);
```
여기서 `num`은 할당할 요소의 개수, `size`는 각 요소의 바이트 크기를 의미한다.

> `size`는 4의 배수로 설정하는 것이 적절하다.

```c
#include <stdio.h>
#include <stdlib.h>

int main() {
	int N = 5;
    int* arr1 = (int*)malloc(N * sizeof(int));  // 초기화되지 않음
    int* arr2 = (int*)calloc(N, sizeof(int));  // 0으로 초기화됨
    
    printf("malloc() 사용: ");
    for (int i = 0; i < N; i++) {
        printf("%d ", arr1[i]);  // 쓰레기 값이 나올 가능성 있음
    }
    printf("\n");
    
    printf("calloc() 사용: ");
    for (int i = 0; i < N; i++) {
        printf("%d ", arr2[i]);  // 항상 0으로 초기화됨
    }
    printf("\n");
    
    free(arr1);
    free(arr2);
    
    return 0;
}
```

`malloc()`과 `calloc()` 함수의 차이점은 다음과 같다.

|함수|메모리 초기화|사용 목적|
|:-:|---|---|
|`malloc(size)`|초기화되지 않음 (쓰레기 값)|빠르게 메모리 할당, 초기화 불필요한 경우|
|`calloc(num, size)`|`0`으로 초기화됨|다중 요소 할당 시 안전한 초기화 필요할 경우|

배열의 크기를 할당하는 코드는 다음과 같다.
```c
#include <stdio.h>
#include <stdlib.h>

int main() {
    int rows = 3, cols = 4;
    
	// 2차원 배열 동적 할당
    int** arr = (int**)calloc(rows, sizeof(int*));  // int*: 8바이트
    for (int i = 0; i < rows; i++) {
        arr[i] = (int*)calloc(cols, sizeof(int));
    }
    
	// 값 입력 및 출력
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            arr[i][j] = i * cols + j;
            printf("%d ", arr[i][j]);
        }
        printf("\n");
    }
    
    // 메모리 해제
    for (int i = 0; i < rows; i++) {
        free(arr[i]);
    }
    free(arr);
    
    return 0;
}
```

### `realloc()`
이미 할당된 메모리 블록의 크기를 변경하는 함수이다. 기존에 할당된 메모리 블록을 새로운 크기로 확장하거나 축소할 수 있다.

크기를 확장할 때 기존 데이터를 그대로 유지하며 새로운 메모리 블록을 할당한다. 기존 메모리 크기를 변경하면서 새로운 메모리 주소를 반환하기 때문에, 변경된 포인터를 반드시 저장해야 한다.

```c
void* realloc(void* ptr, size_t size);
```
`ptr`은 기존에 할당된 메모리 블록의 포인터를 의미하며, `size`는 새로운 메모리 블록의 크기를 의미한다. 성공하면 새로운 메모리 블록의 포인터를 반환한다.

```c
#include <stdio.h>
#include <stdlib.h>

int main() {
	int N = 5;
    int* arr = (int*)malloc(N * sizeof(int));  // 초기 메모리 할당
    
    if (arr == NULL) {
        printf("메모리 할당 실패\n");
        return 0;
    }
    
    // 초기값 설정
    for (int i = 0; i < N; i++) {
        arr[i] = i + 1;
    }
    printf("초기 배열: ");
    for (int i = 0; i < N; i++) {
        printf("%d ", arr[i]);
    }
    printf("\n");
    
    // 메모리 크기 변경 (5 → 10)
    N = 10;
    int* new_arr = (int*)realloc(arr, N * sizeof(int));
    
    if (new_arr == NULL) {
        printf("메모리 재할당 실패\n");
        return 0;
    }
    
    arr = new_arr;  // 새로운 메모리 블록으로 갱신
    // 추가된 공간 초기화
    for (int i = 5; i < N; i++) {
        arr[i] = (i + 1) * 10;
    }
    
    printf("크기 변경 후 배열: ");
    for (int i = 0; i < N; i++) {
        printf("%d ", arr[i]);
    }
    printf("\n");
    
    free(arr);  // 메모리 해제
    // free(new_arr)  // 같은 주소의 값을 해제하므로 오류
    
    return 0;
}
```

```c
int* new_arr = (int*)realloc(arr, N * sizeof(int));
```
해당 코드에서 `realloc()`의 역할은 다음과 같다.
- `arr`가 가리키는 메모리를 확장할 수 있으면 그대로 확장
  - 만약 기존 메모리 블록에서 크기를 조정할 수 있다면, 그 위치에서 크기를 늘리거나 줄임.
  - `new_arr`는 기존의 `arr`과 동일한 메모리 주소를 가리킴.
- 확장이 불가능하면 새로운 메모리 블록을 할당하고 기존 데이터를 복사한 후, 기존 메모리를 해제
  - 새로운 위치에 더 큰 메모리 블록을 할당.
  - 기존 데이터(`arr`의 내용)를 새 블록으로 복사.
  - 기존 `arr`의 메모리를 자동으로 해제.
  - `new_arr`는 새로운 메모리 블록을 가리키고, 기존의 `arr` 주소는 더 이상 유효하지 않음.

해당 내용은 <a href="https://en.cppreference.com/w/c/memory/realloc" target="_blank">realloc</a>에서 확인할 수 있다.