# TIL 250307

**다우기술 FITs 3기 임광영**

**학습 날짜 : 2025년 03월 07일 금요일**

---

## 실습
```c
#include <stdio.h>
#include <stdbool.h>
#include <conio.h>
#define _USE_MATH_DEFINES
#include <math.h>

void practice01();
void practice02();
void practice03();
void practice04();
void practice05();
void practice06();
void practice07();
void practice08();
void practice09();
void practice10();
void practice22();
void practice001();
void practice002();
void practice003();
void practice004();
void practice006();
void practice007();
void practice008();
void practice009();
void printTitle();
void printDays();
bool leapYear(int year);
void days(int year);
int func006(int num);
int func007(int num);
int func008(int num);
int func009(int base, int exp);

int main() {
	//practice01();
	//practice02();
	//practice03();
	//practice04();
	//practice05();
	//practice06();
	//practice07();
	//practice08();
	//practice09();
	//practice10();
	//practice22();
	//practice001();
	//practice002();
	//practice003();
	//practice004();
	//practice006();
	//practice007();
	//practice008();
	practice009();

	return 0;
}

void practice01() {
	int score = 0;

	char military;
	char isMarried;
	int childNum;

	printf("성별을 입력하시오. (M / W): ");
	char gender = getchar();

	if (gender == 'M') {
		printf("군제대 여부를 입력하시오. (T / F): ");
		military = getchar();

		printf("기혼 여부를 입력하시오. (T / F): ");
		isMarried = getchar();

		if (military == 'T') {
			score++;
			if (isMarried == 'T') {
				score++;
			}
		}
	} else {
		printf("기혼 여부를 입력하시오. (T / F): ");
		isMarried = getchar();

		printf("자녀 수를 입력하시오.: ");
		scanf_s("%d", &childNum);

		if (isMarried == 'T') {
			score++;
			if (childNum == 2) {
				score += 2;
			}
		}
	}

	printf("가산점 점수: %d", score);
}

void practice02() {
	int n;
	double total = 0;

	printf("임의의 정수 n을 입력하시오.: ");
	scanf_s("%d", &n);

	for (int i = 2; i <= n; i++) {
		total += ((double)(i - 1)) / i;

		printf("%d%c%d", i - 1, '/', i);
		if (i < n) {
			printf(" + ");
		} else {
			printf(" = ");
		}
	}
	printf("%.1f", total);

}

void practice03() {
	int total = 0;
	int fail = 0;

	for (int i = 1; i <= 10; i++) {
		float score = 0;
		printf("%d번 퀴즈 점수는?: ", i);

		scanf_s("%f", &score);
		total += score;
		if (score < 70) {
			fail++;
		}
	}

	printf("======================\n");
	printf("평균: %.2f\n", (float)total / 10);
	printf("통과자 %d명\n", total - fail);
	printf("탈락자 %d명\n", fail);
}

void practice04() {
	float area = 100;
	int cnt = 0;

	while (area >= 1) {
		area /= 2;
		cnt++;
		printf("%d번 접었을 때의 넓이는 %.2f\n", cnt, area);
	}
}

void practice05() {
	int n;

	printf("출력을 원하는 단을 입력하시오.: ");
	scanf_s("%d", &n);

	for (int i = 1; i <= 9; i++) {
		printf("%d X %d = %2d\n", n, i, n * i);
	}
}

void practice06() {
	printf("===============\n");
	printf("주사위1 주사위2\n");
	printf("---------------\n");
	for (int i = 1; i <= 6; i++) {
		printf("  %d       %d\n", i, 7 - i);
	}
	printf("---------------\n");
	printf("총 6가지");

}

void practice07() {
	char alphabet = 'z';

	for (int i = 0; i <= 25; i++) {
		for (int j = 0; j < 25 - i; j++) {
			printf(" ");
		}
		for (int j = 0; j <= i; j++) {
			printf("%c", alphabet - (i - j));
		}
		printf("\n");
	}
}

void practice08() {
	int n;
	int total = 1;

	printf("정수 n을 입력하시오.: ");
	scanf_s("%d", &n);

	for (int i = 1; i <= n; i++) {
		total *= i;
	}
	printf("1부터 %d까지의 곱은 %d입니다.", n, total);
}

void practice09() {
	float amount = 100;
	int n;

	printf("현재 인의 양은 100mg이입니다.\n");

	printf("몇 일째 되는 날의 인의 최소 잔량을 알고 싶은지 입력하시오.: ");
	scanf_s("%d", &n);

	for (int i = 1; i <= n / 14; i++) {
		amount /= 2;
		printf("%d일까지의 인의 최소 잔량은 %.1fmg입니다.\n", 14 * i, amount);
	}
	printf("따라서 %d일까지의 인의 최소 잔량은 %.1fmg입니다.", n, amount);
}

void practice10() {
	int diff = 'A' - 'a';
	char small;
	
	while (1) {
		printf("알파벳 소문자를 입력하시오.: ");
		// getchar(): 입력 버퍼를 사용
		// _getch(): 입력 버퍼를 사용하지 않음
		// _getche(): _getch()와 동일하며 입력값을 보여줌
		small = _getche();  

		if (small == 0x1b) {
			break;
		}
		if (small >= 'a' && small <= 'z') {
			printf("\n변환된 대문자는 %c입니다.\n", small + diff);
		} else {
			printf("소문자를 입력하시오.");
		}
	}
}

void practice22() {
	int num1, num2;
	int tmp;

	while (1) {
		printf("두 개의 정수를 입력하시오.: ");
		scanf_s("%d %d", &num1, &num2);

		if (num1 < num2) {
			printf("큰 수와 작은 수 순으로 입력하시오.\n");
			continue;
		}

		while (num2) {
			tmp = num1 % num2;
			num1 = num2;
			num2 = tmp;
		}
		printf("최대 공약수는 %d입니다.", num1);
		break;
	}
}

void practice001() {
	int degrees[] = { 0, 30, 45, 60, 90, 120, 135, 150, 180 };

	int size = sizeof(degrees) / sizeof(int);

	for (int i = 0; i < size; i++) {
		printf("sin(%d) = %lf\n", degrees[i], sin(M_PI / 180 * degrees[i]));
	}
}

void practice002() {
	int workHour;
	int wages = 0;

	printf("근로 시간을 입력하시오.: ");
	scanf_s("%d", &workHour);

	wages += (workHour / 8) * 100000;
	wages += (workHour % 8) * 10000;
	printf("총 하루 임금은 %d원입니다.", wages);
}

void practice003() {
	printTitle();
	printDays();
}

void practice004() {	
	int year;

	printf("년도를 입력하시오.: ");
	scanf_s("%d", &year);

	days(year);
}

void practice006() {
	int n;

	printf("n을 입력하시오.: ");
	scanf_s("%d", &n);

	printf("1부터 %d까지의 합: %d", n, func006(n));
}

void practice007() {
	int n;

	printf("n을 입력하시오.: ");
	scanf_s("%d", &n);

	printf("%d! = %d", n, func007(n));
}

void practice008() {
	int n;

	printf("n을 입력하시오.: ");
	scanf_s("%d", &n);
	
	for (int i = 0; i <= n; i++) {
		printf("%d ", func008(i));
	}
}


void practice009() {
	int b, e;

	printf("정수를 입력하시오.: ");
	scanf_s("%d", &b);
	printf("곱할 횟수를 입력하시오.: ");
	scanf_s("%d", &e);

	printf("정답은 %d", func009(b, e));
}

void printTitle() {
	printf("====================\n");
	printf("일 월 화 수 목 금 토\n");
	printf("--------------------\n");
}

void printDays() {
	for (int i = 2; i <= 31; i += 7) {
		for (int j = 0; j <= 6; j++) {
			printf("%2d ", i + j);
		}
		printf("\n");
	}
}

bool leapYear(int year) {
	return (year % 4 == 0 && year % 100 != 0 || year % 400 == 0);
}

void days(int year) {
	bool ans = leapYear(year);
	if (ans) {
		printf("%d년은 366일까지 있습니다.", year);
	} else {
		printf("%d년은 365일까지 있습니다.", year);
	}
}

int func006(int num) {
	if (num == 1) {
		return num;
	}
	return num + func006(num - 1);
}

int func007(int num) {
	if (num == 2) {
		return num;
	}
	return num * func007(num - 1);
}

int func008(int num) {
	if (num == 0) {
		return 0;
	}
	if (num == 1) {
		return 1;
	}
	return func008(num - 2) + func008(num - 1);;
}

int func009(int base, int exp) {
	static int total009 = 1;

	if (exp == 0) return total009;

	total009 *= base;
	return func009(base, exp - 1);
}
```

> `getchar()`: 입력 버퍼를 사용
`_getch()`: 입력 버퍼를 사용하지 않음
`_getche()`: `_getch()`와 동일하며 입력값을 보여줌

## 입력 버퍼
C 언어에서 입력을 받을 때(`scanf()`, `getchar()` 등), 입력 데이터는 입력 버퍼(`stdin` 버퍼)에 먼저 저장된다. 표준 입력(`stdin`) 버퍼는 기본적으로 운영체제가 제공하며, 키보드 입력을 먼저 저장한 후 프로그램이 읽을 때 제공하는 역할을 한다.

### 입력 버퍼 구조
입력 버퍼는 FIFO(First In, First Out) 구조를 가졌다. 사용자가 키보드로 입력한 값은 순서대로 입력 버퍼에 저장되고, 프로그램이 읽을 때 차례대로 꺼내어 사용한다.

```c
#include <stdio.h>

int main() {
    char a, b, c;
    
    printf("문자 3개를 입력하세요: ");
    scanf("%c %c %c", &a, &b, &c);

    printf("첫 번째 문자: %c\n", a);
    printf("두 번째 문자: %c\n", b);
    printf("세 번째 문자: %c\n", c);

    return 0;
}
```
```
문자 3개를 입력하세요: A B C
첫 번째 문자: A
두 번째 문자: B
세 번째 문자: C
```
해당 코드의 경우 사용자가 `A B C`를 입력하고 `Enter`를 누르면 입력 버퍼에 `A B C\n`가 저장된다. `scanf("%c %c %c")`가 버퍼에서 하나씩 데이터를 꺼내와 순서대로 `a`, `b`, `c`에 저장한다.

### 입력 버퍼 처리
```c
#include <stdio.h>

int main() {
    char a, b;

    printf("문자 하나 입력: ");
    scanf("%c", &a);
    // while (getchar() != '\n');  // 입력 버퍼 지우기

    printf("두 번째 문자 입력: ");
    b = getchar();

    printf("첫 번째 문자: %c\n", a);
    printf("두 번째 문자: %c\n", b);

    return 0;
}
```
```
문자 하나 입력: A
두 번째 문자 입력:
첫 번째 문자: A
두 번째 문자:
```
해당 코드의 경우 두 번째 `getchar()`가 입력 버퍼에 남아 있는 `\n`을 가져오게 된다. 따라서 별도로 버퍼를 지울 필요가 있다.

### 숫자에서의 입력 버퍼
숫자를 입력 받을 경우 공백 문자(`Space`, `Tab`, `Enter`)는 자동으로 무시하되, 입력 버퍼를 자동으로 지우지는 않는다.

```c
#include <stdio.h>

int main() {
    int num;

    printf("정수를 입력하세요: ");
    scanf("%d", &num);

    printf("입력된 값: %d\n", num);

    return 0;
}
```
```
정수를 입력하세요: 42
입력된 값: 42
```
해당 코드에서 `42`가 입력되면 `scanf("%d", &num);`이 정수를 읽고, `num = 42`로 저장된다. 하지만 `scanf()`는 숫자 다음에 있는 `Enter`를 읽지 않고, 입력 버퍼에 남겨둔다.
이후 `getchar()` 같은 입력을 받을 경우 남아 있는 `\n`이 읽힐 가능성이 있다.

```c
#include <stdio.h>

int main() {
    int num;
    char ch;

    printf("정수를 입력하세요: ");
    scanf("%d", &num);

    printf("문자를 입력하세요: ");
    ch = getchar();

    printf("입력된 값: %d, 문자: %c\n", num, ch);

    return 0;
}
```
```
정수를 입력하세요: 42
문자를 입력하세요: 
입력된 값: 42, 문자:
```
`scanf("%d", &num);`는 `42`를 읽고, `\n`이 입력 버퍼에 남는다. 그후 `getchar();`가 실행될 때 남아 있던 `\n`을 가져가게 된다.

## 함수

### 함수의 종류
함수의 종류는 다음과 같다.
- 표준함수 : 내장함수 또는 시스템 제공 함수라고도 하며 미리 정의되어 있는 프로그래머가 호출하여 사용할 수 있는 함수
- 헤더파일 : C 언어에서 미리 만들어 놓은 함수가 정의되어 있는 파일
- 사용자 정의 함수 : 사용자가 특정 작업을 수행하기 위해 작성하는 함수로 사용자가 필요에 
의해 함수를 정의하고 사용하는 함수

### 함수의 구조
배개변수를 사용하여 전달하며, Call By Value이다.

> 포인터를 사용하는 경우 Call by Address.

```c
반환타입 함수명(자료형 인수1, 자료형 인수2) {
    return 반환값;
}
```

### 같은 이름의 함수
하나의 이름으로 반환하는 값이나 함수로 전달하는 값이 다른 함수를 만드려면 함수의 프로토
타입에 parameter가 다른 함수를 모두 선언하여야 한다. 이때 C++로 컴파일한다.
```c
#include <stdio.h>

//  int sum() 함수의 프로토타입
int sum(int a, int b);
//float sum() 함수의 프로토타입
float sum(float a, float b);
    
int main() {
	int i = 10,j = 20, k;
 	float a = 3.2, b = 4.5, c;
    
 	//sum 함수의 호출 및 결과 출력
	k = sum(i, j);
 	printf("%d + %d = %d\n", i, j, k);
    
 	//sum 함수의 호출 및 결과 출력
	c = sum(a, b);
 	printf("%f + %f = %f\n", a, b, c);
    
 	return 0;
}
 
int sum(int a, int b) {
	return a + b;
}
 
float sum(float a, float b) {
	return a + b;
}
```

### 말줄임 기호(`...`)
```c
#include <stdio.h>

int sum(int a, ...);

int main() {
	int i = 10, j = 21, k = 30;
    
    sum(i, j);
    
    return  0;
 }
 
int sum(int a, ...) {
	int *i, b;
	i = &a;  // a의 주소
 	a = *i;  // 해당 주소의 값
 	i = i + 2;  // 2 * sizeof(int)만큼 주소 증가
 	b = *i;
    
 	printf("%d + %d = %d", a, b, a + b);
    return a + b;
}
```