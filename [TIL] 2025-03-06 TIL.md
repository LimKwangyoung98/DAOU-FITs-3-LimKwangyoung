# TIL 250306

**다우기술 FITs 3기 임광영**

**학습 날짜 : 2025년 03월 06일 목요일**

---

## 실습

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

void practice06_01() {
	char gender;
	int age;
	float height;

	printf("성별은? (남자라면 M / 여자라면 F) >>> ");
	scanf("%c", &gender);

	printf("나이는? >>> ");
	scanf("%d", &age);

	printf("키는? >>> ");
	scanf("%f", &height);

	printf("\n");
	printf("==========\n");
	printf("성별: %c\n", gender);
	printf("나이: %d세\n", age);
	printf("키: %.1fcm\n", height);
	printf("\n");
}

void practice06_02() {
	char name[10];  // 배열
	char gender;

	printf("이름은? >>> ");
	scanf_s("%s", name, 10);
	getchar();

	printf("성별은? (남자라면 M / 여자라면 F) >>> ");
	gender = getchar();

	printf("%s\n", name);
	putchar(gender);
}

void practice06_03() {
	int num1, num2, num3;

	printf("두 개의 정수를 입력하세요. >>> ");
	scanf("%d %d", &num1, &num2);
	printf("%d %d\n", num1, num2);

	printf("세 개의 정수를 입력하세요. >>> ");
	scanf("%d %d %d", &num1, &num2, &num3);
	printf("%d %d %d\n", num1, num2, num3);
}

void main() {
	practice06_01();
    practice06_02();
    practice06_03();
}
```

> 입력 버퍼 처리는 다음과 같다.
> 
| 형식 지정자 | 데이터 구분 기준 | 개행(`\n`) 입력 시 처리 방식 |
|---|---|---|
| `%d` (정수) | 공백(Space, Tab, Enter)에서 입력 종료 | 무시 (다음 입력을 기다림) |
| `%f` (실수) | 공백(Space, Tab, Enter)에서 입력 종료 | 무시 (다음 입력을 기다림) |
| `%c` (문자) | 공백(Enter 포함)도 문자로 처리됨 | `\n`을 읽을 가능성이 있음 |
| `%s` (문자열) | 공백(Space, Tab, Enter)에서 입력 종료 | 개행(`\n`) 입력 시 문자열 종료 |

## 비트 연산자

하드웨어의 제어에 사용되며 비트논리연산자와 시프트연산자가 있다.