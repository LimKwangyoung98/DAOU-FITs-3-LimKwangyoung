# TIL 250317

**다우기술 FITs 3기 임광영**

**학습 날짜 : 2025년 03월 17일 월요일**

---

## 구조체

구조체가 함수의 인수로 사용될 때 구조체 포인터, 구조체 배열, 구조체 리스트 등으로 활용된다.

### 함수와 구조체
함수의 인수로 구조체를 전달할 수 있다. 이때 구조체를 전역 변수 영역에 선언해야 한다. 함수의 인수로 구조체가 전달되면 구조체의 모든 멤버가 복사되어 함수로 전달된다.

즉 구조체는 하나의 형으로 취급되기 때문에 주소에 의한 호출이 아니라 값에 의한 호출로 처리된다.

### 구조체 포인터
함수의 매개변수로 구조체 주소를 이용한 구조체 포인터를 사용할 수 있다. 이는 주소만 넘겨주면 되므로 멤버의 복사에 시간이 걸리지 않는다. 주소만 전달하기 때문에, 구조체의 값이 바뀌면 호출한 함수의 구조체 값 또한 변경된다.

일반 구조체의 멤버 접근 연산자는 `.` 연산자이며, 구조체 포인터를 사용한 멤버 접근은 `->` 연산자를 사용한다.

`.` 연산자를 통해 일반 구조체의 멤버를 접근한 코드는 다음과 같다.
```c
typedef struct student {
	char name[20];
	char sex;
	int stid;
	int sub1;
	int sub2;
	int sub3;
	double avg;
} ST;

void input(ST st);

void main() {
	ST st1 = { "kdhong", 'm', 1508001, 0, 0, 0, 0 };
	input30(st1);
}

void input(ST st) {
	printf("%s 학생의 3과목 성적을 입력하시오.(공란으로 구분): ", st.name);
	scanf("%d %d %d", &st.sub1, &st.sub2, &st.sub3);
	printf("input() 함수에서 입력된 값은 %d, %d, %d입니다.\n", st.sub1, st.sub2, st.sub3);
}
```

`->` 연산자를 통해 구조체 포인터의 멤버를 접근한 코드는 다음과 같다.
```c
typedef struct student {
	char name[20];
	char sex;
	int stid;
	int sub1;
	int sub2;
	int sub3;
	double avg;
} ST;

void input(ST *st);

void main() {
	ST st1 = { "kdhong", 'm', 1508001, 0, 0, 0, 0 };
	input(&st1);
}

void input(ST *st) {
	printf("%s 학생의 3과목 성적을 입력하시오.(공란으로 구분): ", st->name);
	scanf("%d %d %d", &st->sub1, &st->sub2, &st->sub3);
	printf("input() 함수에서 입력된 값은 %d, %d, %d입니다.\n", st->sub1, st->sub2, st->sub3);
}
```

> `St st[5]`와 같이 구조체를 배열처럼 사용할 수 있다.

### 동적 할당
다음의 구조체를 보자.
```c
typedef struct {
	char name[100];
	double investment_amount;
	double return_rate;
} Investment;

typedef struct {
	char name[100];
	int num_investments;
	Investment* investments;
} Customer;
```
해당 경우 `Customer` 구조체의 `investments` 변수는 포인터 변수이기 때문에 정적 할당이 아닌 동적 할당을 활용해야 한다.

> 만약 `Investment investments[10];`과 같이 선언한다면 정적 할당이므로 인덱스를 활용하여 접근할 수 있다.

`investments` 변수에 새로운 값을 추가해야할 경우 `realloc` 함수를 활용하여 메모리를 재할당하고, `Investment` 구조체를 추가해야 한다.
```c
cus->investments = realloc(cus->investments, (cus->num_investments + 1) * sizeof(Investment));
```
이와 같이 `Investment` 구조체에 해당하는 메모리를 재할당한 뒤 값을 추가한다.
```c
strcpy(cus->investments[cus->num_investments].name, inv.name);
cus->investments[cus->num_investments].investment_amount = inv.investment_amount;
cus->investments[cus->num_investments].return_rate = inv.return_rate / 100;

cus->num_investments++;
```

### 공용체
하나의 데이터에 대해 서로 다른 형태의 데이터로 사용하는 것이다. 모든 멤버들이 같은 메모리 영역을 공유하는 특별한 형태의 구조체로, 멤버를 가진 공용체가 하나의 값을 유지한다.

### 열거형
순서가 있는 데이터들을 열거하여 하나의 형으로 정의하는 것이다. 예약어 `enum`을 사용한다.