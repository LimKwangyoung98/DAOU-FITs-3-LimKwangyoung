# TIL 250313

**다우기술 FITs 3기 임광영**

**학습 날짜 : 2025년 03월 13일 목요일**

---

## `void`와 `const void*`

### `void*`
타입이 정해지지 않은 포인터로, 모든 데이터 타입을 받을 수 있다.

### `$const void*`
포인터가 가리키는 값을 변경할 수 없다.

## `qsort()`에서의 비교 함수
`compare()` 함수는 `qsort()`에서 두 개의 값을 비교하는 역할을 한다. 반드시 `const void*` 타입을 사용해야 한다.

## 가변 길이 배열 문제와 해결법

### `int dim; int arr[dim][dim];`의 문제점
C89/C90에서는 변수 크기 배열(VLA) 사용 불가능하다. C99 이후부터 VLA 지원되지만, MSVC(Visual Studio)에서는 지원되지 않는다.

### 해결 방법
**(1) `#define` 또는 `const int` 사용**
```c
#define DIM 3
int arr[DIM][DIM];
```

**(2) 충분한 크기의 배열을 선언 후 일부 사용**
```c
#define MAX_SIZE 100
int arr[MAX_SIZE][MAX_SIZE];
```