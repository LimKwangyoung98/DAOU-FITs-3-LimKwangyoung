# TIL 250320

**다우기술 FITs 3기 임광영**

**학습 날짜 : 2025년 03월 20일 화요일**

---

## 실습

### 계좌 이체
계좌 이체 실습 코드는 다음과 같다.
```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdint.h>

#define FILENAME "accounts.txt"
#define TMP_FILENAME "tmp.txt"

typedef struct {
	int account_id;
	char name[51];
	double balance;
} Account;

#define WITHDRWAL_MASK 0x01
#define DEPOSIT_MASK 0x02

void create_account();
void read_accounts();
void search_account();
void deposit();
void withdraw();
void transfer();

int practice01() {
	char option;

	while (1) {
		printf("[은행 계좌 시스템]\n");
		printf("1. 신규 계좌 개설 (N)\n");
		printf("2. 계좌 목록 출력 (L)\n");
		printf("3. 계좌 검색      (S)\n");
		printf("4. 입금           (D)\n");
		printf("5. 출금           (W)\n");
		printf("6. 계좌 이체      (T)\n");
		printf("7. 종료           (Q)\n");

		printf("선택: ");
		scanf(" %c", &option);
		printf("\n");

		switch (option) {
		case 'N': case 'n': create_account(); break;
		case 'L': case 'l': read_accounts(); break;
		case 'S': case 's': search_account(); break;
		case 'D': case 'd': deposit(); break;
		case 'W': case 'w': withdraw(); break;
		case 'T': case 't': transfer(); break;
		case 'Q': case 'q': return 0;
		default: printf("다시 입력하시오.\n");
		}
		printf("\n");
	}
}

void create_account() {
	FILE* file = fopen(FILENAME, "a");
	if (!file) return;

	Account account;

	printf("계좌 ID: ");
	scanf("%d", &account.account_id);

	printf("계좌 소유자 이름: ");
	scanf("%s", account.name);

	printf("초기 잔액: ");
	scanf("%lf", &account.balance);

	if (account.balance >= 0) {
		fprintf(file, "%d %s %.2lf\n", account.account_id, account.name, account.balance);
		printf(">> 계좌가 성공적으로 개설되었습니다.\n");
	}
	else {
		printf(">> 초기 잔액을 다시 입력해주세요.\n");
	}
	fclose(file);
}

void read_accounts() {
	FILE* file = fopen(FILENAME, "r");
	if (!file) return;

	Account account;

	char line[100];
	printf("[계좌 목록]\n");
	while (fgets(line, sizeof(line), file)) {
		sscanf(line, "%d %s %lf", &account.account_id, account.name, &account.balance);

		printf("ID : %d | 이름 : %s | 잔액 : %.2lf원\n", account.account_id, account.name, account.balance);
	}
	fclose(file);
}

void search_account() {
	FILE* file = fopen(FILENAME, "r");
	if (!file) return;

	Account account;

	int search_id;
	printf("검색할 계좌 ID: ");
	scanf("%d", &search_id);

	char line[100];
	int flag = 0;
	while (fgets(line, sizeof(line), file)) {
		sscanf(line, "%d %s %lf", &account.account_id, account.name, &account.balance);
		
		if (search_id == account.account_id) {
			printf("ID: %d | 이름 : %s | 잔액 : %.2lf원\n", account.account_id, account.name, account.balance);
			flag = 1;
			break;
		}
	}

	if (!flag) printf(">> 해당 계좌 ID를 찾을 수 없습니다.\n");
	fclose(file);
}

void deposit() {
	FILE* origin = fopen(FILENAME, "r");
	FILE* tmp = fopen(TMP_FILENAME, "w");
	if (!origin) return;

	Account account;

	int deposit_id;
	double deposit_balance;
	printf("입금할 계좌 ID: ");
	scanf("%d", &deposit_id);
	printf("입금할 금액: ");
	scanf("%lf", &deposit_balance);

	if (deposit_balance < 0) {
		printf(">> 입금할 금액을 다시 입력해주세요.\n");
		fclose(origin);
		fclose(tmp);

		remove(TMP_FILENAME);
		return;
	}

	char line[100];
	int flag = 0;
	while (fgets(line, sizeof(line), origin)) {
		sscanf(line, "%d %s %lf", &account.account_id, account.name, &account.balance);

		if (deposit_id == account.account_id) {
			account.balance += deposit_balance;
			flag = 1;
			printf(">> %d번 계좌에서 %.2lf원이 입금되었습니다.\n", account.account_id, deposit_balance);
		}
		fprintf(tmp, "%d %s %.2lf\n", account.account_id, account.name, account.balance);
	}

	if (!flag) printf(">> 해당 계좌 ID를 찾을 수 없습니다.\n");
	fclose(origin);
	fclose(tmp);

	remove(FILENAME);
	rename(TMP_FILENAME, FILENAME);
}

void withdraw() {
	FILE* origin = fopen(FILENAME, "r");
	FILE* tmp = fopen(TMP_FILENAME, "w");
	if (!origin || !tmp) return;

	Account account;

	int withdraw_id;
	double withdraw_balance;
	printf("출금할 계좌 ID: ");
	scanf("%d", &withdraw_id);
	printf("출금할 금액: ");
	scanf("%lf", &withdraw_balance);

	if (withdraw_balance < 0) {
		printf(">> 출금할 금액을 다시 입력해주세요.\n");
		fclose(origin);
		fclose(tmp);

		remove(TMP_FILENAME);
		return;
	}

	char line[100];
	int flag = 0;
	while (fgets(line, sizeof(line), origin)) {
		sscanf(line, "%d %s %lf", &account.account_id, account.name, &account.balance);

		if (withdraw_id == account.account_id) {
			flag = 1;
			if (account.balance >= withdraw_balance) {
				account.balance -= withdraw_balance;
				printf(">> %d번 계좌에서 %.2lf원이 출금되었습니다.\n", account.account_id, withdraw_balance);
			}
			else {
				printf(">> %d번 계좌의 잔액이 부족합니다.\n", account.account_id);
			}
		}
		fprintf(tmp, "%d %s %.2lf\n", account.account_id, account.name, account.balance);
	}

	if (!flag) printf(">> 해당 계좌 ID를 찾을 수 없습니다.\n");
	fclose(origin);
	fclose(tmp);

	remove(FILENAME);
	rename(TMP_FILENAME, FILENAME);
}

void transfer() {
	FILE* origin = fopen(FILENAME, "r");
	FILE* tmp = fopen(TMP_FILENAME, "w");
	if (!origin || !tmp) return;

	Account account;

	int withdraw_id, deposit_id;
	double transfer_balance;
	printf("송금할 계좌 ID: ");
	scanf("%d", &withdraw_id);
	printf("수취할 계좌 ID: ");
	scanf("%d", &deposit_id);
	printf("이체 금액: ");
	scanf("%lf", &transfer_balance);

	if (transfer_balance < 0) {
		printf(">> 이체할 금액을 다시 입력해주세요.\n");
		fclose(origin);
		fclose(tmp);

		remove(TMP_FILENAME);
		return;
	}

	char line[100];
	uint8_t flags = 0;
	while (fgets(line, sizeof(line), origin)) {
		sscanf(line, "%d %s %lf", &account.account_id, account.name, &account.balance);

		if (withdraw_id == account.account_id && account.balance >= transfer_balance) {
			account.balance -= transfer_balance;
			flags |= WITHDRWAL_MASK;
		}

		if (deposit_id == account.account_id) {
			account.balance += transfer_balance;
			flags |= DEPOSIT_MASK;
		}

		fprintf(tmp, "%d %s %.2lf\n", account.account_id, account.name, account.balance);
	}
	
	fclose(origin);
	fclose(tmp);
	if (flags & WITHDRWAL_MASK && flags & DEPOSIT_MASK) {
		printf(">> 계좌 이체가 성공하였습니다.\n");
		remove(FILENAME);
		rename(TMP_FILENAME, FILENAME);
	}
	else {
		printf(">> 계좌 이체가 실패하였습니다.\n");
		remove(TMP_FILENAME);
	}
}
```

## 파일 함수

### `fopen()`
파일을 열어서 읽기, 쓰기, 추가 모드로 파일을 다룰 수 있도록 파일 포인터를 반환한다.

```c
FILE* fopen(const char* filename, const char* mode);
```
- `filename` : 열 파일의 이름
- `mode` : 파일을 여는 모드

`mode` 값으로는 다음과 같다.

|모드|설명|
|:-:|:-:|
|`r`|읽기 모드 (파일이 존재해야 함)|
|`w`|쓰기 모드 (파일이 없으면 생성, 있으면 덮어씀)|
|`a`|추가 모드 (파일이 없으면 생성, 있으면 내용 추가)|
|`rb`|바이너리 읽기 모드|
|`wb`|바이너리 쓰기 모드|
|`ab`|바이너리 추가 모드|

```c
#include <stdio.h>

int main() {
    FILE* file = fopen("file_name.txt", "r");
    if (!file) {
        printf("파일을 열 수 없습니다.\n");
        return 1;
    }
    
    printf("파일이 성공적으로 열렸습니다.\n");
    fclose(file);
    
    return 0;
}
```
`fclose()` 함수를 통해 파일을 닫아서 사용한 리소스를 해제하고 데이터 손실을 방지한다.

### `fprintf()`
파일에 형식을 지정하여 데이터를 출력할 수 있다.

```c
int fprintf(FILE* stream, const char* format, ...);
```
- `stream` : 파일 포인터
- `format` : 형식 지정 문자열

```c
#include <stdio.h>

int main() {
    FILE* file = fopen("file_name.txt", "w");
    if (!file) {
        printf("파일을 열 수 없습니다.\n");
        return 1;
    }

    fprintf(file, "ID: %d, Name: %s, Score: %.2f\n", 101, "Alice", 95.5);
    fclose(file);
    
    return 0;
}
```

`file_name.txt`는 다음과 같은 내용이 저장된다.
```txt
ID: 101, Name: Alice, Score: 95.50
```

### `fscanf()`
파일에서 데이터를 읽어 형식에 맞춰 변수에 저장한다.

```c
int fscanf(FILE* stream, const char *format, ...);
```
- `stream` : 파일 포인터
- `format` : 형식 지정 문자열

`fscanf()` 함수의 반환값은 읽은 항목의 개수이다.

```c
#include <stdio.h>

int main() {
    FILE* file = fopen("file_name.txt", "r");
    if (!file) {
        printf("파일을 열 수 없습니다.\n");
        return 1;
    }

    int id;
    char name[50];
    double score;

    fscanf(file, "ID: %d, Name: %s, Score: %lf", &id, name, &score);
    printf("ID: %d, Name: %s, Score: %.2f\n", id, name, score);

    fclose(file);
    
    return 0;
}
```

출력 결과는 다음과 같다.
```bash
ID: 101, Name: Alice, Score: 95.50
```

만약 파일의 내용이 여러 줄일 경우 `while` 문을 활용한다.
```c
#include <stdio.h>

int main() {
    FILE* file = fopen("file_name.txt", "r");
    if (!file) {
        printf("파일을 열 수 없습니다.\n");
        return 1;
    }

    int id;
    char name[50];
    double score;

    while (fscanf(file, "%d %s %lf", &id, name, &score) == 3) {
        printf("ID: %d, Name: %s, Score: %.2f\n", id, name, score);
    }

    fclose(file);
    
    return 0;
}
```

### `feof()`
파일의 끝(EOF: End Of File)에 도달했는지 확인한다.

```c
int feof(FILE *stream);
```
- `stream` : 확인할 파일 포인터
- 파일 끝이면 `1`, 그렇지 않으면 `0` 반환

```c
#include <stdio.h>

int main() {
    FILE* file = fopen("file_name.txt", "r");
    if (!file) {
        printf("파일을 열 수 없습니다.\n");
        return 1;
    }

    char line[100];
    while (!feof(file)) {
        if (fgets(line, sizeof(line), file)) {
            printf("%s", line);
        }
    }

    fclose(file);
    
    return 0;
}
```

### `fputc()`
파일에 한 문자(`char`)를 기록하는 함수이다. `fprintf()`처럼 형식을 지정하지 않고 그냥 한 글자씩 출력한다.

```c
int fputc(int ch, FILE* stream);
```
- `ch` : 출력할 문자
- `stream` : 파일 포인터
- 반환값: 출력한 문자, 실패 시 `EOF`

```c
#include <stdio.h>

int main() {
    FILE* file = fopen("file_name.txt", "w");
    if (!file) {
        printf("파일을 열 수 없습니다.\n");
        return 1;
    }

    char text[] = "Hello, World!";
    for (int i = 0; text[i] != '\0'; i++) {
        fputc(text[i], file);
    }

    fclose(file);
    
    return 0;
}
```

`file_name.txt` 파일은 다음과 같은 내용이 저장된다.
```txt
Hello, World!
```

### `fgetc()`
파일에서 한 문자(`char`)를 읽는 함수이다. 파일을 한 글자씩 읽어서 문자 단위로 처리할 때 사용한다.

```c
int fgetc(FILE* stream);
```
- `stream` : 읽을 파일 포인터
- 반환값 : 읽은 문자, 파일 끝에 도달하면 `EOF` 반환

```c
#include <stdio.h>

int main() {
    FILE* file = fopen("file_name.txt", "r");
    if (!file) {
        printf("파일을 열 수 없습니다.\n");
        return 1;
    }

    int ch;
    while ((ch = fgetc(file)) != EOF) {
        putchar(ch);
    }

    fclose(file);
    
    return 0;
}
```

> **파일 복사**
`fputc()`와 `fgetc()`를 사용하여, 한 글자씩 파일을 읽고 다른 파일에 복사할 수 있다.
```c
#include <stdio.h>
>
int main() {
    FILE *source = fopen("file_name.txt", "r");
    FILE *destination = fopen("copy.txt", "w");
>
    if (!source || !destination) {
        printf("파일을 열 수 없습니다.\n");
        return 1;
    }
>
    int ch;
    while ((ch = fgetc(source)) != EOF) {
        fputc(ch, destination);
    }
>
    fclose(source);
    fclose(destination);
 >   
    return 0;
}
```

### `fgets()`
파일에서 개행 문자(`\n`)를 포함하여 한 줄씩 읽는다. `size - 1`개의 문자 또는 개행 문자(`\n`)를 만날 때까지 읽고, 문자열 끝에 `\0`을 자동 추가한다.

파일 끝에 도달하면 `NULL`을 반환한다.

```c
char* fgets(char* str, int size, FILE* stream);
```
- `str` : 읽은 문자열을 저장할 버퍼
- `size` : 읽을 최대 문자 개수 (`size - 1`까지 읽고 `\0` 추가)
- `stream` : 파일 포인터

```c
#include <stdio.h>

int main() {
    FILE* file = fopen("file_name.txt", "r");
    if (!file) {
        printf("파일을 열 수 없습니다.\n");
        return 1;
    }

    char line[100];
    while (fgets(line, sizeof(line), file)) {
        printf("%s", line);
    }

    fclose(file);
    
    return 0;
}
```
한 줄씩 읽으며 개행(`\n`)이 포함된다. 따라서 `printf("%s", line);`을 하면 자동으로 줄 바꿈이 유지된다.

### `fputs()`
파일에 한 줄을 출력하는 함수이다. 문자열 끝에 `\n`을 자동 추가하지 않으므로, 개행을 포함하려면 수동으로 `\n`을 추가해야 한다.

```c
int fputs(const char* str, FILE* stream);
```
- `str` : 파일에 출력할 문자열
- `stream` : 파일 포인터
- 반환값 : 성공 시 `0` 이상, 실패 시 `EOF`

```c
int main() {
    FILE* file = fopen("file_name.txt", "w");
    if (!file) {
        printf("파일을 열 수 없습니다.\n");
        return 1;
    }

    fputs("Hello, World!\n", file);
    fputs("This is a test file.\n", file);
    fputs("C programming is fun!\n", file);

    fclose(file);

    return 0;
}
```
`output.txt`에 저장된 내용은 다음과 같다.
```txt
Hello, World!
This is a test file.
C programming is fun!
```

> **`sscanf()`**
문자열에서 데이터를 추출하여 변수에 저장한다. 파일이 아니라 문자열을 분석한다.
>
```c
int sscanf(const char* str, const char* format, ...);
```
- `str` : 입력 문자열
- `format` : 형식 지정 문자열
>
```c
#include <stdio.h>
>
int main() {
    char data[] = "ID: 101, Name: Alice, Score: 95.50";
    int id;
    char name[50];
    double score;
>
    sscanf(data, "ID: %d, Name: %s, Score: %lf", &id, name, &score);
    printf("ID: %d, Name: %s, Score: %.2f\n", id, name, score);
>
    return 0;
}
```
출력 결과는 다음과 같다.
```bash
ID: 101, Name: Alice, Score: 95.50
```

### `fscanf()` vs `fgets()` + `sscanf()`
`fscanf()`는 파일에서 데이터를 직접 변수에 저장하며, 공백이나 개행 문자(`\n`)를 구분자로 사용한다. 파일을 스트림처럼 읽으며 형식화된 입력을 자동으로 처리.하고, 숫자나 문자열 등의 형식을 지정하여 한 번에 읽을 수 있다.

`fgets()`는 파일에서 한 줄 전체(`\n` 포함)를 문자열로 읽으며, `sscanf()`는 문자열에서 형식화된 데이터를 추출한다.
`fscanf()`와 달리 입력 데이터를 한 번에 받아 처리할 수 있어 안정성이 높다.

|비교 항목|`fscanf()`|`fgets() + sscanf()`|
|---|---|---|
|입력 방식|파일에서 직접 형식화된 데이터 읽기|한 줄을 문자열로 읽고 `sscanf()`로 처리|
|공백 처리|자동으로 공백을 구분자로 사용|공백 포함 가능 (`fgets()`는 전체 줄을 읽음)|
|개행 문자 (`\n`)| 자동 제거됨|`\n` 포함됨 (`strcspn()`으로 제거 가능)|
|데이터 안정성|형식이 일치하지 않으면 오류 발생 가능|형식이 다르면 무시 가능 (더 안정적)|
|빈 줄 처리|빈 줄을 자동으로 무시|`fgets()`로 읽은 후 처리 가능|
|에러 처리|형식이 다르면 `fscanf()`가 멈출 수 있음|`fgets()`로 읽고 `sscanf()`로 유연하게 처리 가능|

> `fscanf()` 함수의 속도가 더 빠르지만 공백을 유연하게 처리하지 못한다는 단점이 있다.

### `fwrite()`
메모리에서 파일로 데이터를 블록 단위로 저장하는 함수로, 바이너리 형식으로 데이터를 저장한다.

```c
size_t fwrite(const void* ptr, size_t size, size_t count, FILE* stream);
```
- `ptr` : 저장할 데이터가 있는 메모리 주소
- `size` : 한 개 데이터의 크기 (바이트)
- `count` : 저장할 데이터 개수
- `stream` : 파일 포인터
- 반환값 : 실제로 저장된 데이터 개수

```c
#include <stdio.h>

typedef struct {
    int id;
    char name[20];
    double score;
} Student;

int main() {
    FILE* file = fopen("file_name.dat", "wb");
    if (!file) {
        printf("파일을 열 수 없습니다.\n");
        return 1;
    }

    Student s1 = {101, "Alice", 95.5};
    Student s2 = {102, "Bob", 88.7};
    
    fwrite(&s1, sizeof(Student), 1, file);
    fwrite(&s2, sizeof(Student), 1, file);

    fclose(file);

return 0;
}
```
구조체 데이터를 그대로 저장하므로 텍스트가 아니라 바이너리 형태로 저장되며, `fprintf()`보다 파일 크기가 작고 데이터 손실이 적다.

### `fread()`
파일에서 데이터를 블록 단위로 읽어 메모리에 저장하는 함수이다. `fscanf()`와 달리 형식을 지정할 필요 없이 저장된 그대로 읽는다.

```c
size_t fread(void* ptr, size_t size, size_t count, FILE* stream);
```
- `ptr` : 데이터를 저장할 메모리 주소
- `size` : 한 개 데이터의 크기 (바이트)
- `count` : 읽을 데이터 개수
- `stream` : 파일 포인터
- 반환값 : 실제로 읽은 데이터 개수

```c
#include <stdio.h>

typedef struct {
    int id;
    char name[20];
    double score;
} Student;

int main() {
    FILE* file = fopen("file_name.dat", "rb");
    if (!file) {
        printf("파일을 열 수 없습니다.\n");
        return 1;
    }

    Student s;
    
    while (fread(&s, sizeof(Student), 1, file)) {
        printf("ID: %d, Name: %s, Score: %.2f\n", s.id, s.name, s.score);
    }

    fclose(file);
    
    return 0;
}
```
파일에 저장된 구조체 데이터를 그대로 읽어오기 때문에 변환 과정 없이 빠르다.

> `fwrite()`와 `fread()`는 메모리에 저장된 데이터를 그대로 파일에 기록하거나 읽는 함수로, 모든 연속적인 메모리 블록을 저장 가능하다.
>
|데이터 타입|예제|
|---|---|
|기본 자료형<br>(`int`, `double`, `float`, `char`)|`fwrite(&num, sizeof(int), 1, file);`|
|배열<br>(`int[]`, `double[]`, `char[]`)|`fwrite(arr, sizeof(arr[0]), count, file);`|
|문자열 (`char*`)|`fwrite(str, sizeof(char), strlen(str), file);`|
|구조체 (`struct`)| `fwrite(&s, sizeof(Student), 1, file);` |
|동적 메모리 (`malloc`)|`fwrite(ptr, sizeof(int), size, file);`|
|포인터 자체<br>(`int*`, `double*`)|불가능 (메모리 주소만 저장됨)|
|연결 리스트, 트리 등<br>비연속 데이터|불가능 (연속된 메모리가 아님)|

## 랜덤 접근

파일의 임의의 위치에서 바로 읽기/쓰기를 할 수 있는 접근 방식을 임의 접근이라 한다. 파일 위치 지시자를 조작하는 함수를 사용해야 한다.

### `fseek`
파일의 특정 위치로 포인터를 이동하는 함수이다. 파일을 처음부터 끝까지 순차적으로 읽지 않고, 원하는 위치에서 데이터를 읽거나 쓸 수 있다. 이는 바이너리 파일(`.bin`, `.dat`)을 다룰 때 특히 유용하다.
```c
int fseek(FILE* stream, long offset, int origin);
```
- `stream` : 파일 포인터
- `offset` : 이동할 바이트 수
- `origin` : 기준 위치
  - `SEEK_SET` : 파일의 시작.
  - `SEEK_CUR` : 현재 위치.
  - `SEEK_END` : 파일의 끝.
- 반환값 : 성공 시 `0`, 실패 시 `0`이 아닌 값

```c
#include <stdio.h>

int main() {
    FILE* file = fopen("file_name.txt", "r");
    if (!file) {
        printf("파일을 열 수 없습니다.\n");
        return 1;
    }

    fseek(file, 5, SEEK_SET);
    // fseek(file, 3, SEEK_CUR);
    // fseek(file, -10, SEEK_EMD);
    
    char ch = fgetc(file);

    printf("6번째 문자: %c\n", ch); 

    fclose(file);
    
    return 0;
}
```

### `ftell()`
현재 파일 포인터의 위치(바이트 단위)를 반환한다. `fseek()`와 함께 사용하면 파일 내 위치를 쉽게 추적할 수 있다.
```c
long ftell(FILE* stream);
```
- `stream` : 파일 포인터
- 반환값: 파일의 현재 위치 (바이트 단위, 실패 시 `-1L`)

```c
#include <stdio.h>

int main() {
    FILE* file = fopen("file_name.txt", "r");
    if (!file) {
        printf("파일을 열 수 없습니다.\n");
        return 1;
    }

    printf("초기 위치: %ld\n", ftell(file));

    fseek(file, 10, SEEK_SET);
    printf("이동 후 위치: %ld\n", ftell(file));

    fclose(file);
    
    return 0;
}
```
해당 코드의 결과는 다음과 같다.
```bash
초기 위치: 0
이동 후 위치: 10
```
