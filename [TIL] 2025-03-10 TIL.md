# TIL 250310

**다우기술 FITs 3기 임광영**

**학습 날짜 : 2025년 03월 10일 월요일**

---

## `#define`

### 매크로 상수
상수에 이름을 붙이며 이를 통해 소스의 가독성이 향상되고 일괄 수정이 쉽다.

```c
#define 매크로명 실제값
```

### 매크로 함수
일반 함수처럼 인수를 갖는다. 컴파일을 할 때 함수가 호출되는 개념이 아니라 호출된 곳에 코드가 직접 삽입되기 때문에 컴파일 속도가 빨라진다.

```c
#define 매크로함수명(인수1, 인수2, ...) (치환할 내용 함수내용)
```

주의할 점은 치환할 내용(함수 내용)에 포함된 인수는 반드시 `()`를 해줘야 한다는 것이다. 해당 코드를 보자.
```c
#include <stdio.h>

#define power1(x)  ( x * x )
#define power2(x)  ( (x) * (x) )

int main() {
	int num;
    
 	printf(“정수를 입력하세요: ”);
 	scanf_s(“%d”, &num);
    
 	printf(“%d의 제곱: %d\n”, num, power1(num) );
 	printf(“%d의 제곱: %d\n”, num, power2(num) );
 	printf(“(%d+2)의 제곱: %d\n”, num, power1(num+2) );
 	printf(“%d의 제곱: %d\n”, num, power2(num+2) )
```
```
정수를 입력하세요: 15
15의 제곱: 225
15의 제곱: 225
(15+2)의 제곱: 47
(15+2)의 제곱: 289
```
`power1(x)` 매크로 함수의 경우 `15 + 2 * 15 + 2`로 연산이 되기 때문에 `47`의 값이 나온다. 따라서 치환 인수에 `()`을 주의한다.