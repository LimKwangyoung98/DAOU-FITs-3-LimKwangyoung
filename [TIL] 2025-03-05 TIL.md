# TIL 250305

**다우기술 FITs 3기 임광영**

**학습 날짜 : 2025년 03월 05일 수요일**

---

## 하드웨어

### CPU 내부 구조
![](https://velog.velcdn.com/images/pyoung/post/5dcf9920-5ef5-4e8a-841b-12831ff5f534/image.png)

**ALU(산술논리연산장치)**
각종 산술/논리 연산들을 수행하는 회로이다.
- 산술 연산 : 덧셈 / 뺄셈 / 곱셈 / 나눗셈
- 논리 연산 : `AND` / `OR` / `NO`

**Register(레지스터)**
CPU 내에서 데이터들을 기억하는 Memory 장치이다. 엑세스 속도가 가장 빠르며, 내부 회로가 복잡해서 많은 공간을 차지한다.

레지스터 종류로는 다음과 같다.
- 프로그램 카운터(PC, Program Counter)
  - 명령어의 주소를 보유하고 있는 레지스터.
  - 명령어 인출 후, 해당 명령어 길이만큼 증가.
  - 분기(branch) 명령어가 실행될 경우, 해당 목적지 주소로 갱신.
- 누산기(AC, Accumulator)
  - 데이터의 연산 결과를 일시적으로 저장하는 레지스터.
  - AC의 비트 수 = word 길이(CPU가 한 번에 처리할 수 있는 비트 수).
- 명령어 레지스터(IR, Instruction Register)
  - 현재 실행중인 명령어를 저장하는 레지스터.
- 기억장치 주소 레지스터(MAR, Memory Address Register)
  - 접근할 메모리의 주소를 임시 저장.
  - PC에 저장된 명령어 주소가 System 주소 버스로 출력되기 전에 일시적으로 저장.
  
## 소프트웨어

### 운영 소프트웨어 (OS)
윈도우, 리눅스, 유닉스, 맥 등이 있다. 하드웨어와 응용 소프트웨어를 관리한다.

작성 언어로는 C 언어가 있다.

### 응용 소프트웨어 (AP)
운영 소프트웨어를 제외한 소프트웨어를 말한다.

작성 언어로는 Java, Python, JavaScript, C 언어 등이 있다.

> 응용 소프트웨어와 하드웨어를 연결하는 것이 운영 소프트웨어이다.

## C 언어 실습

### 기본 문법
`#include`를 통해 라이브러리를 불러올 수 있다.

해당 라이브러리 코드를 보면 다음과 같은 특징이 있다.
- `__` : 시스템/컴파일러가 예약한 것이므로 사용자가 직접 정의해서 사용하면 안됨
- `#` : C 컴파일러가 본격적으로 코드를 컴파일하기 전에 전처리를 수행하는 부분
- `#pragma once` : 헤더 파일이 여러 번 포함되는 것을 방지

```c
// 외부 라이브러리 추가
#include <stdio.h>
#include "day01.h"  // 사용자 정의 헤더파일

int main() {
	printf("Hellow World\n");

	// 문자: 작은 따옴표, 문자열: 큰 따옴표
	printf("abc0\n");
	printf("%c\n", 'a');

	return 0;
}
```
```c
#include <stdio.h>

int main() {
	// int 정수형 변수 (default: signed)
	signed int a;  // 음수에서 양수
	unsigned int ua = 0;  // 양수
	a = -2147483647;
	printf("\n%d\t", a - 2);  // 2147483647

	unsigned short b = 65000;
	printf("%d", b);  // 65000
	printf("\n");
    
	return 0;
}
```

> `a = -2147483648;` 코드는 오류를 발생시킨다. `signed int`의 범위는 `-2147483648`부터 `2147483647`이지만 `a = -2147483648`는 `a = -(2147483648)`의 순서로 처리되기 때문에 `signed int`의 범위를 벗어나게 된다.

```c
#include <stdio.h>

int main() {
	// 문자형 변수
	char c1 = 'a';
	printf("\n%c\t%d", c1, c1);  // a	97

	char spaceBarDec = 32;
	printf("\nA%cB", spaceBarDec);
	char spaceBarHex = 0x20;
	printf("\nA%cB", spaceBarHex);
	char spaceBarOct = 0040;
	printf("\nA%cB", spaceBarOct);
	printf("\n");
    
    return 0;
}
```
```c
#include <stdio.h>

int main() {
	// long 정수형 변수 (default: signed)
	long a10 = 2000L;
	printf("\n%d", 10);
	printf("\n");

	// sizeof(): 연산자 메모리 크기 출력
	printf("\nchar %d byte.", sizeof(short));
	printf("\nchar %d byte.", sizeof(int));
	printf("\nchar %d byte.", sizeof(long));

	return 0;
}
```
```c
#include <stdio.h>

int main() {
	// 실수형 변수
	double dd1 = 1.4e+3, dd2 = -0.005, dd3 = 3.14;
	float f1 = 1.45, f2 = 1.25f, f3 = 0.5f;
	printf("\n%4.2lf", dd1);  // double 형일 경우 .lf
	printf("\n%lf", dd2);  // 기본적으로는 6자리
	printf("\n%f", f1);  // float 형일 경우 .f
	printf("\n%.3f", f2);  // float 형일 경우 .f
	printf("\n");

	printf("\ndouble %d byte.", sizeof(double));
	printf("\nfloat %d byte.", sizeof(float));

	return 0;
}
```
```c
#include <stdio.h>

int main() {
	// double과 float는 가수부의 범위가 다르다.
	double dd4 = 1.1234567890123456;
	float f4 = 1.1234567890123456f;
	printf("\ndouble형 소수점 표현 %.15lf", dd4);  // 소수점 14자리까지 정확
	printf("\nfloat형 소수점 표현 %.15f", f4);  // 소수점 6자리까리 정확
}
```
```c
#include <stdio.h>

int main() {
	int a, b;
	printf("정수를 입력하세요 >> ");
	scanf_s("%d %d", &a, &b);
	printf("\n출력: %d와 %d", a, b);

	double c, d;
	printf("\n실수를 입력하세요 >> ");
	scanf_s("%lf %lf", &c, &d);
	printf("\n출력: %lf와 %lf", c, d);

	char e, f;
	printf("\n문자를 입력하세요 >> ");
	//scanf_s("%c %c", &e, &f);
	e = getchar();  // 문자 한 개를 키보드로 입력 받기
	getchar();  // Enter로 입력 받기
	f = getchar();
	printf("\n출력: %c와 %c", e, f);

	return 0;
}
```

### DLL 파일
DLL(Dynamic Link Library)은 다른 프로그램에서 공통으로 사용할 수 있는 코드 및 데이터를 포함하는 파일이다.	

DLL 파일을 사용하면 다음과 같은 장점이 있다.
- 메모리 절약 : 동일한 DLL을 여러 프로그램에서 공유 가능
- 업데이트 용이: 프로그램을 다시 컴파일하지 않고도 DLL만 교체하여 기능을 수정 가능
- 실행 파일 크기 감소: 실행 파일에서 직접 함수를 포함하지 않으므로 크기가 작아짐
- 플러그인 기능 가능: 프로그램 실행 중 특정 DLL을 불러와 기능을 추가 가능

***
<a href="https://github.com/okskna/ComputerArchitecture_Summary" target="_blank">컴퓨터 구조론 GitHub</a>